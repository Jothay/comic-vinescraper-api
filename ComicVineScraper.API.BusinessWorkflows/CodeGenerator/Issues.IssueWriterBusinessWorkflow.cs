// <auto-generated>
// <copyright file="Issues.IIssueWriterModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the IssueWriter model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Issues
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class IssueWritersBusinessWorkflow : IIssueWritersBusinessWorkflow
    {
        public IssueWritersBusinessWorkflow(IIssueWritersRepository issueWritersRepository, IIssueWriterMapper issueWriterMapper)
        {
            IssueWritersRepository = issueWritersRepository;
            IssueWriterMapper = issueWriterMapper;
        }

        #region Private Variables
        private IIssueWriterMapper IssueWriterMapper { get; }
        private IIssueWritersRepository IssueWritersRepository { get; }
        #endregion
        #region Read
        public IIssueWriterModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return IssueWriterMapper.MapToModel(IssueWritersRepository.Get(id));
        }

        public IIssueWriterModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return IssueWriterMapper.MapToModel(IssueWritersRepository.Get(key));
        }

        public List<IIssueWriterModel> Search(IIssueWriterSearchModel searchModel, bool asListing = false)
        {
            var results = IssueWritersRepository.Search(searchModel);
            return asListing
                ? results.Select(IssueWriterMapper.MapToModelListing).ToList()
                : results.Select(IssueWriterMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IIssueWriterModel Create(IIssueWriterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(IssueWriterMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = IssueWriterMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            IssueWritersRepository.Add(newEntity);
            // Try to Save Changes
            IssueWritersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IIssueWriterModel Update(IIssueWriterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = IssueWritersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (IssueWriterMapper.AreEqual(model, existingEntity))
            {
                return IssueWriterMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            IssueWriterMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            IssueWritersRepository.Update(IssueWriterMapper.MapToEntity(model));
            // Try to Save Changes
            IssueWritersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = IssueWritersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = IssueWritersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IIssueWriter entity)
        {
            // Deactivate it
            IssueWritersRepository.Deactivate(entity);
            // Try to Save Changes
            IssueWritersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = IssueWritersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = IssueWritersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IIssueWriter entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            IssueWritersRepository.Remove(entity);
            // Try to Save Changes
            IssueWritersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
