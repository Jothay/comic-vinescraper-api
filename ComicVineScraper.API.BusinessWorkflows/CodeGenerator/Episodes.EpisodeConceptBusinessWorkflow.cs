// <auto-generated>
// <copyright file="Episodes.IEpisodeConceptModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the EpisodeConcept model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Episodes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class EpisodeConceptsBusinessWorkflow : IEpisodeConceptsBusinessWorkflow
    {
        public EpisodeConceptsBusinessWorkflow(IEpisodeConceptsRepository episodeConceptsRepository, IEpisodeConceptMapper episodeConceptMapper)
        {
            EpisodeConceptsRepository = episodeConceptsRepository;
            EpisodeConceptMapper = episodeConceptMapper;
        }

        #region Private Variables
        private IEpisodeConceptMapper EpisodeConceptMapper { get; }
        private IEpisodeConceptsRepository EpisodeConceptsRepository { get; }
        #endregion
        #region Read
        public IEpisodeConceptModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return EpisodeConceptMapper.MapToModel(EpisodeConceptsRepository.Get(id));
        }

        public IEpisodeConceptModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return EpisodeConceptMapper.MapToModel(EpisodeConceptsRepository.Get(key));
        }

        public List<IEpisodeConceptModel> Search(IEpisodeConceptSearchModel searchModel, bool asListing = false)
        {
            var results = EpisodeConceptsRepository.Search(searchModel);
            return asListing
                ? results.Select(EpisodeConceptMapper.MapToModelListing).ToList()
                : results.Select(EpisodeConceptMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IEpisodeConceptModel Create(IEpisodeConceptModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(EpisodeConceptMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = EpisodeConceptMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            EpisodeConceptsRepository.Add(newEntity);
            // Try to Save Changes
            EpisodeConceptsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IEpisodeConceptModel Update(IEpisodeConceptModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = EpisodeConceptsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (EpisodeConceptMapper.AreEqual(model, existingEntity))
            {
                return EpisodeConceptMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            EpisodeConceptMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            EpisodeConceptsRepository.Update(EpisodeConceptMapper.MapToEntity(model));
            // Try to Save Changes
            EpisodeConceptsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeConceptsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeConceptsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IEpisodeConcept entity)
        {
            // Deactivate it
            EpisodeConceptsRepository.Deactivate(entity);
            // Try to Save Changes
            EpisodeConceptsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeConceptsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeConceptsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IEpisodeConcept entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            EpisodeConceptsRepository.Remove(entity);
            // Try to Save Changes
            EpisodeConceptsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
