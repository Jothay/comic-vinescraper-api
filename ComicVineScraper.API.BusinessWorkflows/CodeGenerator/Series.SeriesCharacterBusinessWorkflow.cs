// <auto-generated>
// <copyright file="Series.SeriesCharacterBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the SeriesCharacter business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Series
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class SeriesCharactersBusinessWorkflow : ISeriesCharactersBusinessWorkflow
    {
        public SeriesCharactersBusinessWorkflow(ISeriesCharactersRepository seriesCharactersRepository, ISeriesCharacterMapper seriesCharacterMapper)
        {
            SeriesCharactersRepository = seriesCharactersRepository;
            SeriesCharacterMapper = seriesCharacterMapper;
        }

        #region Private Variables
        private ISeriesCharacterMapper SeriesCharacterMapper { get; }
        private ISeriesCharactersRepository SeriesCharactersRepository { get; }
        #endregion
        #region Read
        public ISeriesCharacterModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return SeriesCharacterMapper.MapToModel(SeriesCharactersRepository.Get(id));
        }

        public ISeriesCharacterModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return SeriesCharacterMapper.MapToModel(SeriesCharactersRepository.Get(key));
        }

        public List<ISeriesCharacterModel> Search(ISeriesCharacterSearchModel searchModel, bool asListing = false)
        {
            var results = SeriesCharactersRepository.Search(searchModel);
            return asListing
                ? results.Select(SeriesCharacterMapper.MapToModelListing).ToList()
                : results.Select(SeriesCharacterMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ISeriesCharacterModel Create(ISeriesCharacterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(SeriesCharacterMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = SeriesCharacterMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            SeriesCharactersRepository.Add(newEntity);
            // Try to Save Changes
            SeriesCharactersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ISeriesCharacterModel Update(ISeriesCharacterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = SeriesCharactersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (SeriesCharacterMapper.AreEqual(model, existingEntity))
            {
                return SeriesCharacterMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            SeriesCharacterMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            SeriesCharactersRepository.Update(SeriesCharacterMapper.MapToEntity(model));
            // Try to Save Changes
            SeriesCharactersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = SeriesCharactersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = SeriesCharactersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ISeriesCharacter entity)
        {
            // Deactivate it
            SeriesCharactersRepository.Deactivate(entity);
            // Try to Save Changes
            SeriesCharactersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = SeriesCharactersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = SeriesCharactersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ISeriesCharacter entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            SeriesCharactersRepository.Remove(entity);
            // Try to Save Changes
            SeriesCharactersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
