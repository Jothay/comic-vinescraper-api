// <auto-generated>
// <copyright file="Concepts.ConceptAliasBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the ConceptAlias business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Concepts
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ConceptAliasesBusinessWorkflow : IConceptAliasesBusinessWorkflow
    {
        public ConceptAliasesBusinessWorkflow(IConceptAliasesRepository conceptAliasesRepository, IConceptAliasMapper conceptAliasMapper)
        {
            ConceptAliasesRepository = conceptAliasesRepository;
            ConceptAliasMapper = conceptAliasMapper;
        }

        #region Private Variables
        private IConceptAliasMapper ConceptAliasMapper { get; }
        private IConceptAliasesRepository ConceptAliasesRepository { get; }
        #endregion
        #region Read
        public IConceptAliasModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ConceptAliasMapper.MapToModel(ConceptAliasesRepository.Get(id));
        }

        public IConceptAliasModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ConceptAliasMapper.MapToModel(ConceptAliasesRepository.Get(key));
        }

        public List<IConceptAliasModel> Search(IConceptAliasSearchModel searchModel, bool asListing = false)
        {
            var results = ConceptAliasesRepository.Search(searchModel);
            return asListing
                ? results.Select(ConceptAliasMapper.MapToModelListing).ToList()
                : results.Select(ConceptAliasMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IConceptAliasModel Create(IConceptAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ConceptAliasMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ConceptAliasMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ConceptAliasesRepository.Add(newEntity);
            // Try to Save Changes
            ConceptAliasesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IConceptAliasModel Update(IConceptAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ConceptAliasesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ConceptAliasMapper.AreEqual(model, existingEntity))
            {
                return ConceptAliasMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ConceptAliasMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ConceptAliasesRepository.Update(ConceptAliasMapper.MapToEntity(model));
            // Try to Save Changes
            ConceptAliasesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ConceptAliasesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ConceptAliasesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IConceptAlias entity)
        {
            // Deactivate it
            ConceptAliasesRepository.Deactivate(entity);
            // Try to Save Changes
            ConceptAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ConceptAliasesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ConceptAliasesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IConceptAlias entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ConceptAliasesRepository.Remove(entity);
            // Try to Save Changes
            ConceptAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
