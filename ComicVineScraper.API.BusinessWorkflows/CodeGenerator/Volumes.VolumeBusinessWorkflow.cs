// <auto-generated>
// <copyright file="Volumes.VolumeBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the Volume business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Volumes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class VolumesBusinessWorkflow : IVolumesBusinessWorkflow
    {
        public VolumesBusinessWorkflow(IVolumesRepository volumesRepository, IVolumeMapper volumeMapper)
        {
            VolumesRepository = volumesRepository;
            VolumeMapper = volumeMapper;
        }

        #region Private Variables
        private IVolumeMapper VolumeMapper { get; }
        private IVolumesRepository VolumesRepository { get; }
        #endregion
        #region Read
        public IVolumeModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return VolumeMapper.MapToModel(VolumesRepository.Get(id));
        }

        public IVolumeModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return VolumeMapper.MapToModel(VolumesRepository.Get(key));
        }

        public List<IVolumeModel> Search(IVolumeSearchModel searchModel, bool asListing = false)
        {
            var results = VolumesRepository.Search(searchModel);
            return asListing
                ? results.Select(VolumeMapper.MapToModelListing).ToList()
                : results.Select(VolumeMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IVolumeModel Create(IVolumeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(VolumeMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = VolumeMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            VolumesRepository.Add(newEntity);
            // Try to Save Changes
            VolumesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IVolumeModel Update(IVolumeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = VolumesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (VolumeMapper.AreEqual(model, existingEntity))
            {
                return VolumeMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            VolumeMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            VolumesRepository.Update(VolumeMapper.MapToEntity(model));
            // Try to Save Changes
            VolumesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IVolume entity)
        {
            // Deactivate it
            VolumesRepository.Deactivate(entity);
            // Try to Save Changes
            VolumesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IVolume entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            VolumesRepository.Remove(entity);
            // Try to Save Changes
            VolumesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
