// <auto-generated>
// <copyright file="Episodes.IEpisodePersonModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the EpisodePerson model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Episodes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class EpisodePeopleBusinessWorkflow : IEpisodePeopleBusinessWorkflow
    {
        public EpisodePeopleBusinessWorkflow(IEpisodePeopleRepository episodePeopleRepository, IEpisodePersonMapper episodePersonMapper)
        {
            EpisodePeopleRepository = episodePeopleRepository;
            EpisodePersonMapper = episodePersonMapper;
        }

        #region Private Variables
        private IEpisodePersonMapper EpisodePersonMapper { get; }
        private IEpisodePeopleRepository EpisodePeopleRepository { get; }
        #endregion
        #region Read
        public IEpisodePersonModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return EpisodePersonMapper.MapToModel(EpisodePeopleRepository.Get(id));
        }

        public IEpisodePersonModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return EpisodePersonMapper.MapToModel(EpisodePeopleRepository.Get(key));
        }

        public List<IEpisodePersonModel> Search(IEpisodePersonSearchModel searchModel, bool asListing = false)
        {
            var results = EpisodePeopleRepository.Search(searchModel);
            return asListing
                ? results.Select(EpisodePersonMapper.MapToModelListing).ToList()
                : results.Select(EpisodePersonMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IEpisodePersonModel Create(IEpisodePersonModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(EpisodePersonMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = EpisodePersonMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            EpisodePeopleRepository.Add(newEntity);
            // Try to Save Changes
            EpisodePeopleRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IEpisodePersonModel Update(IEpisodePersonModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = EpisodePeopleRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (EpisodePersonMapper.AreEqual(model, existingEntity))
            {
                return EpisodePersonMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            EpisodePersonMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            EpisodePeopleRepository.Update(EpisodePersonMapper.MapToEntity(model));
            // Try to Save Changes
            EpisodePeopleRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodePeopleRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodePeopleRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IEpisodePerson entity)
        {
            // Deactivate it
            EpisodePeopleRepository.Deactivate(entity);
            // Try to Save Changes
            EpisodePeopleRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodePeopleRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodePeopleRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IEpisodePerson entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            EpisodePeopleRepository.Remove(entity);
            // Try to Save Changes
            EpisodePeopleRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
