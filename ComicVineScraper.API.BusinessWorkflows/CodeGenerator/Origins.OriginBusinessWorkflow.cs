// <auto-generated>
// <copyright file="Origins.OriginBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the Origin business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Origins
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class OriginsBusinessWorkflow : IOriginsBusinessWorkflow
    {
        public OriginsBusinessWorkflow(IOriginsRepository originsRepository, IOriginMapper originMapper)
        {
            OriginsRepository = originsRepository;
            OriginMapper = originMapper;
        }

        #region Private Variables
        private IOriginMapper OriginMapper { get; }
        private IOriginsRepository OriginsRepository { get; }
        #endregion
        #region Read
        public IOriginModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return OriginMapper.MapToModel(OriginsRepository.Get(id));
        }

        public IOriginModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return OriginMapper.MapToModel(OriginsRepository.Get(key));
        }

        public List<IOriginModel> Search(IOriginSearchModel searchModel, bool asListing = false)
        {
            var results = OriginsRepository.Search(searchModel);
            return asListing
                ? results.Select(OriginMapper.MapToModelListing).ToList()
                : results.Select(OriginMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IOriginModel Create(IOriginModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(OriginMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = OriginMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            OriginsRepository.Add(newEntity);
            // Try to Save Changes
            OriginsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IOriginModel Update(IOriginModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = OriginsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (OriginMapper.AreEqual(model, existingEntity))
            {
                return OriginMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            OriginMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            OriginsRepository.Update(OriginMapper.MapToEntity(model));
            // Try to Save Changes
            OriginsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = OriginsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = OriginsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IOrigin entity)
        {
            // Deactivate it
            OriginsRepository.Deactivate(entity);
            // Try to Save Changes
            OriginsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = OriginsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = OriginsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IOrigin entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            OriginsRepository.Remove(entity);
            // Try to Save Changes
            OriginsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
