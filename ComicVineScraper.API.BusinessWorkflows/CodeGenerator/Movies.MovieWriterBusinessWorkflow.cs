// <auto-generated>
// <copyright file="Movies.MovieWriterBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the MovieWriter business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Movies
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class MovieWritersBusinessWorkflow : IMovieWritersBusinessWorkflow
    {
        public MovieWritersBusinessWorkflow(IMovieWritersRepository movieWritersRepository, IMovieWriterMapper movieWriterMapper)
        {
            MovieWritersRepository = movieWritersRepository;
            MovieWriterMapper = movieWriterMapper;
        }

        #region Private Variables
        private IMovieWriterMapper MovieWriterMapper { get; }
        private IMovieWritersRepository MovieWritersRepository { get; }
        #endregion
        #region Read
        public IMovieWriterModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return MovieWriterMapper.MapToModel(MovieWritersRepository.Get(id));
        }

        public IMovieWriterModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return MovieWriterMapper.MapToModel(MovieWritersRepository.Get(key));
        }

        public List<IMovieWriterModel> Search(IMovieWriterSearchModel searchModel, bool asListing = false)
        {
            var results = MovieWritersRepository.Search(searchModel);
            return asListing
                ? results.Select(MovieWriterMapper.MapToModelListing).ToList()
                : results.Select(MovieWriterMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IMovieWriterModel Create(IMovieWriterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(MovieWriterMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = MovieWriterMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            MovieWritersRepository.Add(newEntity);
            // Try to Save Changes
            MovieWritersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IMovieWriterModel Update(IMovieWriterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = MovieWritersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (MovieWriterMapper.AreEqual(model, existingEntity))
            {
                return MovieWriterMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            MovieWriterMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            MovieWritersRepository.Update(MovieWriterMapper.MapToEntity(model));
            // Try to Save Changes
            MovieWritersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieWritersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieWritersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IMovieWriter entity)
        {
            // Deactivate it
            MovieWritersRepository.Deactivate(entity);
            // Try to Save Changes
            MovieWritersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieWritersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieWritersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IMovieWriter entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            MovieWritersRepository.Remove(entity);
            // Try to Save Changes
            MovieWritersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
