// <auto-generated>
// <copyright file="StoryArcs.IStoryArcModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the StoryArc model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.StoryArcs
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class StoryArcsBusinessWorkflow : IStoryArcsBusinessWorkflow
    {
        public StoryArcsBusinessWorkflow(IStoryArcsRepository storyArcsRepository, IStoryArcMapper storyArcMapper)
        {
            StoryArcsRepository = storyArcsRepository;
            StoryArcMapper = storyArcMapper;
        }

        #region Private Variables
        private IStoryArcMapper StoryArcMapper { get; }
        private IStoryArcsRepository StoryArcsRepository { get; }
        #endregion
        #region Read
        public IStoryArcModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return StoryArcMapper.MapToModel(StoryArcsRepository.Get(id));
        }

        public IStoryArcModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return StoryArcMapper.MapToModel(StoryArcsRepository.Get(key));
        }

        public List<IStoryArcModel> Search(IStoryArcSearchModel searchModel, bool asListing = false)
        {
            var results = StoryArcsRepository.Search(searchModel);
            return asListing
                ? results.Select(StoryArcMapper.MapToModelListing).ToList()
                : results.Select(StoryArcMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IStoryArcModel Create(IStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(StoryArcMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = StoryArcMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            StoryArcsRepository.Add(newEntity);
            // Try to Save Changes
            StoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IStoryArcModel Update(IStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = StoryArcsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (StoryArcMapper.AreEqual(model, existingEntity))
            {
                return StoryArcMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            StoryArcMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            StoryArcsRepository.Update(StoryArcMapper.MapToEntity(model));
            // Try to Save Changes
            StoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = StoryArcsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = StoryArcsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IStoryArc entity)
        {
            // Deactivate it
            StoryArcsRepository.Deactivate(entity);
            // Try to Save Changes
            StoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = StoryArcsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = StoryArcsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IStoryArc entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            StoryArcsRepository.Remove(entity);
            // Try to Save Changes
            StoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
