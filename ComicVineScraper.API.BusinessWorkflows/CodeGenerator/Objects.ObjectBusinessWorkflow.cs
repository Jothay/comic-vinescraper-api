// <auto-generated>
// <copyright file="Objects.ObjectBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the Object business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Objects
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ObjectsBusinessWorkflow : IObjectsBusinessWorkflow
    {
        public ObjectsBusinessWorkflow(IObjectsRepository objectsRepository, IObjectMapper objectMapper)
        {
            ObjectsRepository = objectsRepository;
            ObjectMapper = objectMapper;
        }

        #region Private Variables
        private IObjectMapper ObjectMapper { get; }
        private IObjectsRepository ObjectsRepository { get; }
        #endregion
        #region Read
        public IObjectModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ObjectMapper.MapToModel(ObjectsRepository.Get(id));
        }

        public IObjectModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ObjectMapper.MapToModel(ObjectsRepository.Get(key));
        }

        public List<IObjectModel> Search(IObjectSearchModel searchModel, bool asListing = false)
        {
            var results = ObjectsRepository.Search(searchModel);
            return asListing
                ? results.Select(ObjectMapper.MapToModelListing).ToList()
                : results.Select(ObjectMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IObjectModel Create(IObjectModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ObjectMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ObjectMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ObjectsRepository.Add(newEntity);
            // Try to Save Changes
            ObjectsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IObjectModel Update(IObjectModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ObjectsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ObjectMapper.AreEqual(model, existingEntity))
            {
                return ObjectMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ObjectMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ObjectsRepository.Update(ObjectMapper.MapToEntity(model));
            // Try to Save Changes
            ObjectsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ObjectsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ObjectsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IObject entity)
        {
            // Deactivate it
            ObjectsRepository.Deactivate(entity);
            // Try to Save Changes
            ObjectsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ObjectsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ObjectsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IObject entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ObjectsRepository.Remove(entity);
            // Try to Save Changes
            ObjectsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
