// <auto-generated>
// <copyright file="Volumes.VolumeAliasBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the VolumeAlias business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Volumes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class VolumeAliasesBusinessWorkflow : IVolumeAliasesBusinessWorkflow
    {
        public VolumeAliasesBusinessWorkflow(IVolumeAliasesRepository volumeAliasesRepository, IVolumeAliasMapper volumeAliasMapper)
        {
            VolumeAliasesRepository = volumeAliasesRepository;
            VolumeAliasMapper = volumeAliasMapper;
        }

        #region Private Variables
        private IVolumeAliasMapper VolumeAliasMapper { get; }
        private IVolumeAliasesRepository VolumeAliasesRepository { get; }
        #endregion
        #region Read
        public IVolumeAliasModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return VolumeAliasMapper.MapToModel(VolumeAliasesRepository.Get(id));
        }

        public IVolumeAliasModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return VolumeAliasMapper.MapToModel(VolumeAliasesRepository.Get(key));
        }

        public List<IVolumeAliasModel> Search(IVolumeAliasSearchModel searchModel, bool asListing = false)
        {
            var results = VolumeAliasesRepository.Search(searchModel);
            return asListing
                ? results.Select(VolumeAliasMapper.MapToModelListing).ToList()
                : results.Select(VolumeAliasMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IVolumeAliasModel Create(IVolumeAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(VolumeAliasMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = VolumeAliasMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            VolumeAliasesRepository.Add(newEntity);
            // Try to Save Changes
            VolumeAliasesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IVolumeAliasModel Update(IVolumeAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = VolumeAliasesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (VolumeAliasMapper.AreEqual(model, existingEntity))
            {
                return VolumeAliasMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            VolumeAliasMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            VolumeAliasesRepository.Update(VolumeAliasMapper.MapToEntity(model));
            // Try to Save Changes
            VolumeAliasesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumeAliasesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumeAliasesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IVolumeAlias entity)
        {
            // Deactivate it
            VolumeAliasesRepository.Deactivate(entity);
            // Try to Save Changes
            VolumeAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumeAliasesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumeAliasesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IVolumeAlias entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            VolumeAliasesRepository.Remove(entity);
            // Try to Save Changes
            VolumeAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
