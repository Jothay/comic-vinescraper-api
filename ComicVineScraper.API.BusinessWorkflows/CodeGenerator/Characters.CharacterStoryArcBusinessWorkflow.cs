// <auto-generated>
// <copyright file="Characters.ICharacterStoryArcModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the CharacterStoryArc model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharacterStoryArcsBusinessWorkflow : ICharacterStoryArcsBusinessWorkflow
    {
        public CharacterStoryArcsBusinessWorkflow(ICharacterStoryArcsRepository characterStoryArcsRepository, ICharacterStoryArcMapper characterStoryArcMapper)
        {
            CharacterStoryArcsRepository = characterStoryArcsRepository;
            CharacterStoryArcMapper = characterStoryArcMapper;
        }

        #region Private Variables
        private ICharacterStoryArcMapper CharacterStoryArcMapper { get; }
        private ICharacterStoryArcsRepository CharacterStoryArcsRepository { get; }
        #endregion
        #region Read
        public ICharacterStoryArcModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterStoryArcMapper.MapToModel(CharacterStoryArcsRepository.Get(id));
        }

        public ICharacterStoryArcModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterStoryArcMapper.MapToModel(CharacterStoryArcsRepository.Get(key));
        }

        public List<ICharacterStoryArcModel> Search(ICharacterStoryArcSearchModel searchModel, bool asListing = false)
        {
            var results = CharacterStoryArcsRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterStoryArcMapper.MapToModelListing).ToList()
                : results.Select(CharacterStoryArcMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterStoryArcModel Create(ICharacterStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterStoryArcMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterStoryArcMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharacterStoryArcsRepository.Add(newEntity);
            // Try to Save Changes
            CharacterStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterStoryArcModel Update(ICharacterStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharacterStoryArcsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterStoryArcMapper.AreEqual(model, existingEntity))
            {
                return CharacterStoryArcMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterStoryArcMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharacterStoryArcsRepository.Update(CharacterStoryArcMapper.MapToEntity(model));
            // Try to Save Changes
            CharacterStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterStoryArcsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterStoryArcsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacterStoryArc entity)
        {
            // Deactivate it
            CharacterStoryArcsRepository.Deactivate(entity);
            // Try to Save Changes
            CharacterStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterStoryArcsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterStoryArcsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacterStoryArc entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharacterStoryArcsRepository.Remove(entity);
            // Try to Save Changes
            CharacterStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
