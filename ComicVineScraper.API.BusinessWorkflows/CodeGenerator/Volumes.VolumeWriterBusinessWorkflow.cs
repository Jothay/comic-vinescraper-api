// <auto-generated>
// <copyright file="Volumes.VolumeWriterBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the VolumeWriter business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Volumes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class VolumeWritersBusinessWorkflow : IVolumeWritersBusinessWorkflow
    {
        public VolumeWritersBusinessWorkflow(IVolumeWritersRepository volumeWritersRepository, IVolumeWriterMapper volumeWriterMapper)
        {
            VolumeWritersRepository = volumeWritersRepository;
            VolumeWriterMapper = volumeWriterMapper;
        }

        #region Private Variables
        private IVolumeWriterMapper VolumeWriterMapper { get; }
        private IVolumeWritersRepository VolumeWritersRepository { get; }
        #endregion
        #region Read
        public IVolumeWriterModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return VolumeWriterMapper.MapToModel(VolumeWritersRepository.Get(id));
        }

        public IVolumeWriterModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return VolumeWriterMapper.MapToModel(VolumeWritersRepository.Get(key));
        }

        public List<IVolumeWriterModel> Search(IVolumeWriterSearchModel searchModel, bool asListing = false)
        {
            var results = VolumeWritersRepository.Search(searchModel);
            return asListing
                ? results.Select(VolumeWriterMapper.MapToModelListing).ToList()
                : results.Select(VolumeWriterMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IVolumeWriterModel Create(IVolumeWriterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(VolumeWriterMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = VolumeWriterMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            VolumeWritersRepository.Add(newEntity);
            // Try to Save Changes
            VolumeWritersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IVolumeWriterModel Update(IVolumeWriterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = VolumeWritersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (VolumeWriterMapper.AreEqual(model, existingEntity))
            {
                return VolumeWriterMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            VolumeWriterMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            VolumeWritersRepository.Update(VolumeWriterMapper.MapToEntity(model));
            // Try to Save Changes
            VolumeWritersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumeWritersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumeWritersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IVolumeWriter entity)
        {
            // Deactivate it
            VolumeWritersRepository.Deactivate(entity);
            // Try to Save Changes
            VolumeWritersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumeWritersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumeWritersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IVolumeWriter entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            VolumeWritersRepository.Remove(entity);
            // Try to Save Changes
            VolumeWritersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
