// <auto-generated>
// <copyright file="Concepts.IConceptVolumeModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the ConceptVolume model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Concepts
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ConceptVolumesBusinessWorkflow : IConceptVolumesBusinessWorkflow
    {
        public ConceptVolumesBusinessWorkflow(IConceptVolumesRepository conceptVolumesRepository, IConceptVolumeMapper conceptVolumeMapper)
        {
            ConceptVolumesRepository = conceptVolumesRepository;
            ConceptVolumeMapper = conceptVolumeMapper;
        }

        #region Private Variables
        private IConceptVolumeMapper ConceptVolumeMapper { get; }
        private IConceptVolumesRepository ConceptVolumesRepository { get; }
        #endregion
        #region Read
        public IConceptVolumeModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ConceptVolumeMapper.MapToModel(ConceptVolumesRepository.Get(id));
        }

        public IConceptVolumeModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ConceptVolumeMapper.MapToModel(ConceptVolumesRepository.Get(key));
        }

        public List<IConceptVolumeModel> Search(IConceptVolumeSearchModel searchModel, bool asListing = false)
        {
            var results = ConceptVolumesRepository.Search(searchModel);
            return asListing
                ? results.Select(ConceptVolumeMapper.MapToModelListing).ToList()
                : results.Select(ConceptVolumeMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IConceptVolumeModel Create(IConceptVolumeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ConceptVolumeMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ConceptVolumeMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ConceptVolumesRepository.Add(newEntity);
            // Try to Save Changes
            ConceptVolumesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IConceptVolumeModel Update(IConceptVolumeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ConceptVolumesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ConceptVolumeMapper.AreEqual(model, existingEntity))
            {
                return ConceptVolumeMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ConceptVolumeMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ConceptVolumesRepository.Update(ConceptVolumeMapper.MapToEntity(model));
            // Try to Save Changes
            ConceptVolumesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ConceptVolumesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ConceptVolumesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IConceptVolume entity)
        {
            // Deactivate it
            ConceptVolumesRepository.Deactivate(entity);
            // Try to Save Changes
            ConceptVolumesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ConceptVolumesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ConceptVolumesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IConceptVolume entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ConceptVolumesRepository.Remove(entity);
            // Try to Save Changes
            ConceptVolumesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
