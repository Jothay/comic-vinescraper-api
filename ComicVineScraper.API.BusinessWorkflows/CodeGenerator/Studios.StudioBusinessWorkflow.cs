// <auto-generated>
// <copyright file="Studios.IStudioModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the Studio model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Studios
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class StudiosBusinessWorkflow : IStudiosBusinessWorkflow
    {
        public StudiosBusinessWorkflow(IStudiosRepository studiosRepository, IStudioMapper studioMapper)
        {
            StudiosRepository = studiosRepository;
            StudioMapper = studioMapper;
        }

        #region Private Variables
        private IStudioMapper StudioMapper { get; }
        private IStudiosRepository StudiosRepository { get; }
        #endregion
        #region Read
        public IStudioModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return StudioMapper.MapToModel(StudiosRepository.Get(id));
        }

        public IStudioModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return StudioMapper.MapToModel(StudiosRepository.Get(key));
        }

        public List<IStudioModel> Search(IStudioSearchModel searchModel, bool asListing = false)
        {
            var results = StudiosRepository.Search(searchModel);
            return asListing
                ? results.Select(StudioMapper.MapToModelListing).ToList()
                : results.Select(StudioMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IStudioModel Create(IStudioModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(StudioMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = StudioMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            StudiosRepository.Add(newEntity);
            // Try to Save Changes
            StudiosRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IStudioModel Update(IStudioModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = StudiosRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (StudioMapper.AreEqual(model, existingEntity))
            {
                return StudioMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            StudioMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            StudiosRepository.Update(StudioMapper.MapToEntity(model));
            // Try to Save Changes
            StudiosRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = StudiosRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = StudiosRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IStudio entity)
        {
            // Deactivate it
            StudiosRepository.Deactivate(entity);
            // Try to Save Changes
            StudiosRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = StudiosRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = StudiosRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IStudio entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            StudiosRepository.Remove(entity);
            // Try to Save Changes
            StudiosRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
