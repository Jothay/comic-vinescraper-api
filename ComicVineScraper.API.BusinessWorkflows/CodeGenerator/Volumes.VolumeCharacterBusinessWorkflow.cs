// <auto-generated>
// <copyright file="Volumes.VolumeCharacterBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the VolumeCharacter business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Volumes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class VolumeCharactersBusinessWorkflow : IVolumeCharactersBusinessWorkflow
    {
        public VolumeCharactersBusinessWorkflow(IVolumeCharactersRepository volumeCharactersRepository, IVolumeCharacterMapper volumeCharacterMapper)
        {
            VolumeCharactersRepository = volumeCharactersRepository;
            VolumeCharacterMapper = volumeCharacterMapper;
        }

        #region Private Variables
        private IVolumeCharacterMapper VolumeCharacterMapper { get; }
        private IVolumeCharactersRepository VolumeCharactersRepository { get; }
        #endregion
        #region Read
        public IVolumeCharacterModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return VolumeCharacterMapper.MapToModel(VolumeCharactersRepository.Get(id));
        }

        public IVolumeCharacterModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return VolumeCharacterMapper.MapToModel(VolumeCharactersRepository.Get(key));
        }

        public List<IVolumeCharacterModel> Search(IVolumeCharacterSearchModel searchModel, bool asListing = false)
        {
            var results = VolumeCharactersRepository.Search(searchModel);
            return asListing
                ? results.Select(VolumeCharacterMapper.MapToModelListing).ToList()
                : results.Select(VolumeCharacterMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IVolumeCharacterModel Create(IVolumeCharacterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(VolumeCharacterMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = VolumeCharacterMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            VolumeCharactersRepository.Add(newEntity);
            // Try to Save Changes
            VolumeCharactersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IVolumeCharacterModel Update(IVolumeCharacterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = VolumeCharactersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (VolumeCharacterMapper.AreEqual(model, existingEntity))
            {
                return VolumeCharacterMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            VolumeCharacterMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            VolumeCharactersRepository.Update(VolumeCharacterMapper.MapToEntity(model));
            // Try to Save Changes
            VolumeCharactersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumeCharactersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumeCharactersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IVolumeCharacter entity)
        {
            // Deactivate it
            VolumeCharactersRepository.Deactivate(entity);
            // Try to Save Changes
            VolumeCharactersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumeCharactersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumeCharactersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IVolumeCharacter entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            VolumeCharactersRepository.Remove(entity);
            // Try to Save Changes
            VolumeCharactersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
