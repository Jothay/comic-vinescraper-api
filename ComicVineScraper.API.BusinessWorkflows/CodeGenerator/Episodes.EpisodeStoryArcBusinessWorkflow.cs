// <auto-generated>
// <copyright file="Episodes.IEpisodeStoryArcModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the EpisodeStoryArc model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Episodes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class EpisodeStoryArcsBusinessWorkflow : IEpisodeStoryArcsBusinessWorkflow
    {
        public EpisodeStoryArcsBusinessWorkflow(IEpisodeStoryArcsRepository episodeStoryArcsRepository, IEpisodeStoryArcMapper episodeStoryArcMapper)
        {
            EpisodeStoryArcsRepository = episodeStoryArcsRepository;
            EpisodeStoryArcMapper = episodeStoryArcMapper;
        }

        #region Private Variables
        private IEpisodeStoryArcMapper EpisodeStoryArcMapper { get; }
        private IEpisodeStoryArcsRepository EpisodeStoryArcsRepository { get; }
        #endregion
        #region Read
        public IEpisodeStoryArcModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return EpisodeStoryArcMapper.MapToModel(EpisodeStoryArcsRepository.Get(id));
        }

        public IEpisodeStoryArcModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return EpisodeStoryArcMapper.MapToModel(EpisodeStoryArcsRepository.Get(key));
        }

        public List<IEpisodeStoryArcModel> Search(IEpisodeStoryArcSearchModel searchModel, bool asListing = false)
        {
            var results = EpisodeStoryArcsRepository.Search(searchModel);
            return asListing
                ? results.Select(EpisodeStoryArcMapper.MapToModelListing).ToList()
                : results.Select(EpisodeStoryArcMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IEpisodeStoryArcModel Create(IEpisodeStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(EpisodeStoryArcMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = EpisodeStoryArcMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            EpisodeStoryArcsRepository.Add(newEntity);
            // Try to Save Changes
            EpisodeStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IEpisodeStoryArcModel Update(IEpisodeStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = EpisodeStoryArcsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (EpisodeStoryArcMapper.AreEqual(model, existingEntity))
            {
                return EpisodeStoryArcMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            EpisodeStoryArcMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            EpisodeStoryArcsRepository.Update(EpisodeStoryArcMapper.MapToEntity(model));
            // Try to Save Changes
            EpisodeStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeStoryArcsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeStoryArcsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IEpisodeStoryArc entity)
        {
            // Deactivate it
            EpisodeStoryArcsRepository.Deactivate(entity);
            // Try to Save Changes
            EpisodeStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeStoryArcsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeStoryArcsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IEpisodeStoryArc entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            EpisodeStoryArcsRepository.Remove(entity);
            // Try to Save Changes
            EpisodeStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
