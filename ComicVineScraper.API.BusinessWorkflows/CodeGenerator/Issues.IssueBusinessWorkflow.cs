// <auto-generated>
// <copyright file="Issues.IIssueModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the Issue model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Issues
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class IssuesBusinessWorkflow : IIssuesBusinessWorkflow
    {
        public IssuesBusinessWorkflow(IIssuesRepository issuesRepository, IIssueMapper issueMapper)
        {
            IssuesRepository = issuesRepository;
            IssueMapper = issueMapper;
        }

        #region Private Variables
        private IIssueMapper IssueMapper { get; }
        private IIssuesRepository IssuesRepository { get; }
        #endregion
        #region Read
        public IIssueModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return IssueMapper.MapToModel(IssuesRepository.Get(id));
        }

        public IIssueModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return IssueMapper.MapToModel(IssuesRepository.Get(key));
        }

        public List<IIssueModel> Search(IIssueSearchModel searchModel, bool asListing = false)
        {
            var results = IssuesRepository.Search(searchModel);
            return asListing
                ? results.Select(IssueMapper.MapToModelListing).ToList()
                : results.Select(IssueMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IIssueModel Create(IIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(IssueMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = IssueMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            IssuesRepository.Add(newEntity);
            // Try to Save Changes
            IssuesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IIssueModel Update(IIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = IssuesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (IssueMapper.AreEqual(model, existingEntity))
            {
                return IssueMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            IssueMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            IssuesRepository.Update(IssueMapper.MapToEntity(model));
            // Try to Save Changes
            IssuesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = IssuesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = IssuesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IIssue entity)
        {
            // Deactivate it
            IssuesRepository.Deactivate(entity);
            // Try to Save Changes
            IssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = IssuesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = IssuesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IIssue entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            IssuesRepository.Remove(entity);
            // Try to Save Changes
            IssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
