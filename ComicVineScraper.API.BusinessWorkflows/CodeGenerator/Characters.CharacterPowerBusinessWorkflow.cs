// <auto-generated>
// <copyright file="Characters.CharacterPowerBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the CharacterPower business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharacterPowersBusinessWorkflow : ICharacterPowersBusinessWorkflow
    {
        public CharacterPowersBusinessWorkflow(ICharacterPowersRepository characterPowersRepository, ICharacterPowerMapper characterPowerMapper)
        {
            CharacterPowersRepository = characterPowersRepository;
            CharacterPowerMapper = characterPowerMapper;
        }

        #region Private Variables
        private ICharacterPowerMapper CharacterPowerMapper { get; }
        private ICharacterPowersRepository CharacterPowersRepository { get; }
        #endregion
        #region Read
        public ICharacterPowerModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterPowerMapper.MapToModel(CharacterPowersRepository.Get(id));
        }

        public ICharacterPowerModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterPowerMapper.MapToModel(CharacterPowersRepository.Get(key));
        }

        public List<ICharacterPowerModel> Search(ICharacterPowerSearchModel searchModel, bool asListing = false)
        {
            var results = CharacterPowersRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterPowerMapper.MapToModelListing).ToList()
                : results.Select(CharacterPowerMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterPowerModel Create(ICharacterPowerModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterPowerMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterPowerMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharacterPowersRepository.Add(newEntity);
            // Try to Save Changes
            CharacterPowersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterPowerModel Update(ICharacterPowerModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharacterPowersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterPowerMapper.AreEqual(model, existingEntity))
            {
                return CharacterPowerMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterPowerMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharacterPowersRepository.Update(CharacterPowerMapper.MapToEntity(model));
            // Try to Save Changes
            CharacterPowersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterPowersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterPowersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacterPower entity)
        {
            // Deactivate it
            CharacterPowersRepository.Deactivate(entity);
            // Try to Save Changes
            CharacterPowersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterPowersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterPowersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacterPower entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharacterPowersRepository.Remove(entity);
            // Try to Save Changes
            CharacterPowersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
