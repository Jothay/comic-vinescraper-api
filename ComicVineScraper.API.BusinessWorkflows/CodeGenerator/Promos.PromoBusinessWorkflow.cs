// <auto-generated>
// <copyright file="Promos.PromoBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the Promo business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Promos
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class PromosBusinessWorkflow : IPromosBusinessWorkflow
    {
        public PromosBusinessWorkflow(IPromosRepository promosRepository, IPromoMapper promoMapper)
        {
            PromosRepository = promosRepository;
            PromoMapper = promoMapper;
        }

        #region Private Variables
        private IPromoMapper PromoMapper { get; }
        private IPromosRepository PromosRepository { get; }
        #endregion
        #region Read
        public IPromoModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return PromoMapper.MapToModel(PromosRepository.Get(id));
        }

        public IPromoModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return PromoMapper.MapToModel(PromosRepository.Get(key));
        }

        public List<IPromoModel> Search(IPromoSearchModel searchModel, bool asListing = false)
        {
            var results = PromosRepository.Search(searchModel);
            return asListing
                ? results.Select(PromoMapper.MapToModelListing).ToList()
                : results.Select(PromoMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IPromoModel Create(IPromoModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(PromoMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = PromoMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            PromosRepository.Add(newEntity);
            // Try to Save Changes
            PromosRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IPromoModel Update(IPromoModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = PromosRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (PromoMapper.AreEqual(model, existingEntity))
            {
                return PromoMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            PromoMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            PromosRepository.Update(PromoMapper.MapToEntity(model));
            // Try to Save Changes
            PromosRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = PromosRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = PromosRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IPromo entity)
        {
            // Deactivate it
            PromosRepository.Deactivate(entity);
            // Try to Save Changes
            PromosRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = PromosRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = PromosRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IPromo entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            PromosRepository.Remove(entity);
            // Try to Save Changes
            PromosRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
