// <auto-generated>
// <copyright file="Episodes.EpisodeCharacterDiedBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the EpisodeCharacterDied business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Episodes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class EpisodeCharactersDiedBusinessWorkflow : IEpisodeCharactersDiedBusinessWorkflow
    {
        public EpisodeCharactersDiedBusinessWorkflow(IEpisodeCharactersDiedRepository episodeCharactersDiedRepository, IEpisodeCharacterDiedMapper episodeCharacterDiedMapper)
        {
            EpisodeCharactersDiedRepository = episodeCharactersDiedRepository;
            EpisodeCharacterDiedMapper = episodeCharacterDiedMapper;
        }

        #region Private Variables
        private IEpisodeCharacterDiedMapper EpisodeCharacterDiedMapper { get; }
        private IEpisodeCharactersDiedRepository EpisodeCharactersDiedRepository { get; }
        #endregion
        #region Read
        public IEpisodeCharacterDiedModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return EpisodeCharacterDiedMapper.MapToModel(EpisodeCharactersDiedRepository.Get(id));
        }

        public IEpisodeCharacterDiedModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return EpisodeCharacterDiedMapper.MapToModel(EpisodeCharactersDiedRepository.Get(key));
        }

        public List<IEpisodeCharacterDiedModel> Search(IEpisodeCharacterDiedSearchModel searchModel, bool asListing = false)
        {
            var results = EpisodeCharactersDiedRepository.Search(searchModel);
            return asListing
                ? results.Select(EpisodeCharacterDiedMapper.MapToModelListing).ToList()
                : results.Select(EpisodeCharacterDiedMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IEpisodeCharacterDiedModel Create(IEpisodeCharacterDiedModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(EpisodeCharacterDiedMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = EpisodeCharacterDiedMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            EpisodeCharactersDiedRepository.Add(newEntity);
            // Try to Save Changes
            EpisodeCharactersDiedRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IEpisodeCharacterDiedModel Update(IEpisodeCharacterDiedModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = EpisodeCharactersDiedRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (EpisodeCharacterDiedMapper.AreEqual(model, existingEntity))
            {
                return EpisodeCharacterDiedMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            EpisodeCharacterDiedMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            EpisodeCharactersDiedRepository.Update(EpisodeCharacterDiedMapper.MapToEntity(model));
            // Try to Save Changes
            EpisodeCharactersDiedRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeCharactersDiedRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeCharactersDiedRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IEpisodeCharacterDied entity)
        {
            // Deactivate it
            EpisodeCharactersDiedRepository.Deactivate(entity);
            // Try to Save Changes
            EpisodeCharactersDiedRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeCharactersDiedRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeCharactersDiedRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IEpisodeCharacterDied entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            EpisodeCharactersDiedRepository.Remove(entity);
            // Try to Save Changes
            EpisodeCharactersDiedRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
