// <auto-generated>
// <copyright file="Objects.ObjectAliasBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the ObjectAlias business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Objects
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ObjectAliasesBusinessWorkflow : IObjectAliasesBusinessWorkflow
    {
        public ObjectAliasesBusinessWorkflow(IObjectAliasesRepository objectAliasesRepository, IObjectAliasMapper objectAliasMapper)
        {
            ObjectAliasesRepository = objectAliasesRepository;
            ObjectAliasMapper = objectAliasMapper;
        }

        #region Private Variables
        private IObjectAliasMapper ObjectAliasMapper { get; }
        private IObjectAliasesRepository ObjectAliasesRepository { get; }
        #endregion
        #region Read
        public IObjectAliasModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ObjectAliasMapper.MapToModel(ObjectAliasesRepository.Get(id));
        }

        public IObjectAliasModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ObjectAliasMapper.MapToModel(ObjectAliasesRepository.Get(key));
        }

        public List<IObjectAliasModel> Search(IObjectAliasSearchModel searchModel, bool asListing = false)
        {
            var results = ObjectAliasesRepository.Search(searchModel);
            return asListing
                ? results.Select(ObjectAliasMapper.MapToModelListing).ToList()
                : results.Select(ObjectAliasMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IObjectAliasModel Create(IObjectAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ObjectAliasMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ObjectAliasMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ObjectAliasesRepository.Add(newEntity);
            // Try to Save Changes
            ObjectAliasesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IObjectAliasModel Update(IObjectAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ObjectAliasesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ObjectAliasMapper.AreEqual(model, existingEntity))
            {
                return ObjectAliasMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ObjectAliasMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ObjectAliasesRepository.Update(ObjectAliasMapper.MapToEntity(model));
            // Try to Save Changes
            ObjectAliasesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ObjectAliasesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ObjectAliasesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IObjectAlias entity)
        {
            // Deactivate it
            ObjectAliasesRepository.Deactivate(entity);
            // Try to Save Changes
            ObjectAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ObjectAliasesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ObjectAliasesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IObjectAlias entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ObjectAliasesRepository.Remove(entity);
            // Try to Save Changes
            ObjectAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
