// <auto-generated>
// <copyright file="Movies.MovieConceptBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the MovieConcept business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Movies
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class MovieConceptsBusinessWorkflow : IMovieConceptsBusinessWorkflow
    {
        public MovieConceptsBusinessWorkflow(IMovieConceptsRepository movieConceptsRepository, IMovieConceptMapper movieConceptMapper)
        {
            MovieConceptsRepository = movieConceptsRepository;
            MovieConceptMapper = movieConceptMapper;
        }

        #region Private Variables
        private IMovieConceptMapper MovieConceptMapper { get; }
        private IMovieConceptsRepository MovieConceptsRepository { get; }
        #endregion
        #region Read
        public IMovieConceptModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return MovieConceptMapper.MapToModel(MovieConceptsRepository.Get(id));
        }

        public IMovieConceptModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return MovieConceptMapper.MapToModel(MovieConceptsRepository.Get(key));
        }

        public List<IMovieConceptModel> Search(IMovieConceptSearchModel searchModel, bool asListing = false)
        {
            var results = MovieConceptsRepository.Search(searchModel);
            return asListing
                ? results.Select(MovieConceptMapper.MapToModelListing).ToList()
                : results.Select(MovieConceptMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IMovieConceptModel Create(IMovieConceptModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(MovieConceptMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = MovieConceptMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            MovieConceptsRepository.Add(newEntity);
            // Try to Save Changes
            MovieConceptsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IMovieConceptModel Update(IMovieConceptModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = MovieConceptsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (MovieConceptMapper.AreEqual(model, existingEntity))
            {
                return MovieConceptMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            MovieConceptMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            MovieConceptsRepository.Update(MovieConceptMapper.MapToEntity(model));
            // Try to Save Changes
            MovieConceptsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieConceptsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieConceptsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IMovieConcept entity)
        {
            // Deactivate it
            MovieConceptsRepository.Deactivate(entity);
            // Try to Save Changes
            MovieConceptsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieConceptsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieConceptsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IMovieConcept entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            MovieConceptsRepository.Remove(entity);
            // Try to Save Changes
            MovieConceptsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
