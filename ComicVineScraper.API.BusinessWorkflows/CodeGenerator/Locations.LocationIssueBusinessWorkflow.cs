// <auto-generated>
// <copyright file="Locations.ILocationIssueModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the LocationIssue model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Locations
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class LocationIssuesBusinessWorkflow : ILocationIssuesBusinessWorkflow
    {
        public LocationIssuesBusinessWorkflow(ILocationIssuesRepository locationIssuesRepository, ILocationIssueMapper locationIssueMapper)
        {
            LocationIssuesRepository = locationIssuesRepository;
            LocationIssueMapper = locationIssueMapper;
        }

        #region Private Variables
        private ILocationIssueMapper LocationIssueMapper { get; }
        private ILocationIssuesRepository LocationIssuesRepository { get; }
        #endregion
        #region Read
        public ILocationIssueModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return LocationIssueMapper.MapToModel(LocationIssuesRepository.Get(id));
        }

        public ILocationIssueModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return LocationIssueMapper.MapToModel(LocationIssuesRepository.Get(key));
        }

        public List<ILocationIssueModel> Search(ILocationIssueSearchModel searchModel, bool asListing = false)
        {
            var results = LocationIssuesRepository.Search(searchModel);
            return asListing
                ? results.Select(LocationIssueMapper.MapToModelListing).ToList()
                : results.Select(LocationIssueMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ILocationIssueModel Create(ILocationIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(LocationIssueMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = LocationIssueMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            LocationIssuesRepository.Add(newEntity);
            // Try to Save Changes
            LocationIssuesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ILocationIssueModel Update(ILocationIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = LocationIssuesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (LocationIssueMapper.AreEqual(model, existingEntity))
            {
                return LocationIssueMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            LocationIssueMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            LocationIssuesRepository.Update(LocationIssueMapper.MapToEntity(model));
            // Try to Save Changes
            LocationIssuesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationIssuesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationIssuesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ILocationIssue entity)
        {
            // Deactivate it
            LocationIssuesRepository.Deactivate(entity);
            // Try to Save Changes
            LocationIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationIssuesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationIssuesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ILocationIssue entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            LocationIssuesRepository.Remove(entity);
            // Try to Save Changes
            LocationIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
