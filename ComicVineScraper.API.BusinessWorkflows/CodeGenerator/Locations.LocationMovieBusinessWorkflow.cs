// <auto-generated>
// <copyright file="Locations.LocationMovieBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the LocationMovie business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Locations
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class LocationMoviesBusinessWorkflow : ILocationMoviesBusinessWorkflow
    {
        public LocationMoviesBusinessWorkflow(ILocationMoviesRepository locationMoviesRepository, ILocationMovieMapper locationMovieMapper)
        {
            LocationMoviesRepository = locationMoviesRepository;
            LocationMovieMapper = locationMovieMapper;
        }

        #region Private Variables
        private ILocationMovieMapper LocationMovieMapper { get; }
        private ILocationMoviesRepository LocationMoviesRepository { get; }
        #endregion
        #region Read
        public ILocationMovieModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return LocationMovieMapper.MapToModel(LocationMoviesRepository.Get(id));
        }

        public ILocationMovieModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return LocationMovieMapper.MapToModel(LocationMoviesRepository.Get(key));
        }

        public List<ILocationMovieModel> Search(ILocationMovieSearchModel searchModel, bool asListing = false)
        {
            var results = LocationMoviesRepository.Search(searchModel);
            return asListing
                ? results.Select(LocationMovieMapper.MapToModelListing).ToList()
                : results.Select(LocationMovieMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ILocationMovieModel Create(ILocationMovieModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(LocationMovieMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = LocationMovieMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            LocationMoviesRepository.Add(newEntity);
            // Try to Save Changes
            LocationMoviesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ILocationMovieModel Update(ILocationMovieModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = LocationMoviesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (LocationMovieMapper.AreEqual(model, existingEntity))
            {
                return LocationMovieMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            LocationMovieMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            LocationMoviesRepository.Update(LocationMovieMapper.MapToEntity(model));
            // Try to Save Changes
            LocationMoviesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationMoviesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationMoviesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ILocationMovie entity)
        {
            // Deactivate it
            LocationMoviesRepository.Deactivate(entity);
            // Try to Save Changes
            LocationMoviesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationMoviesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationMoviesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ILocationMovie entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            LocationMoviesRepository.Remove(entity);
            // Try to Save Changes
            LocationMoviesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
