// <auto-generated>
// <copyright file="Teams.ITeamIssueModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the TeamIssue model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Teams
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class TeamIssuesBusinessWorkflow : ITeamIssuesBusinessWorkflow
    {
        public TeamIssuesBusinessWorkflow(ITeamIssuesRepository teamIssuesRepository, ITeamIssueMapper teamIssueMapper)
        {
            TeamIssuesRepository = teamIssuesRepository;
            TeamIssueMapper = teamIssueMapper;
        }

        #region Private Variables
        private ITeamIssueMapper TeamIssueMapper { get; }
        private ITeamIssuesRepository TeamIssuesRepository { get; }
        #endregion
        #region Read
        public ITeamIssueModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return TeamIssueMapper.MapToModel(TeamIssuesRepository.Get(id));
        }

        public ITeamIssueModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return TeamIssueMapper.MapToModel(TeamIssuesRepository.Get(key));
        }

        public List<ITeamIssueModel> Search(ITeamIssueSearchModel searchModel, bool asListing = false)
        {
            var results = TeamIssuesRepository.Search(searchModel);
            return asListing
                ? results.Select(TeamIssueMapper.MapToModelListing).ToList()
                : results.Select(TeamIssueMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ITeamIssueModel Create(ITeamIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(TeamIssueMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = TeamIssueMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            TeamIssuesRepository.Add(newEntity);
            // Try to Save Changes
            TeamIssuesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ITeamIssueModel Update(ITeamIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = TeamIssuesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (TeamIssueMapper.AreEqual(model, existingEntity))
            {
                return TeamIssueMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            TeamIssueMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            TeamIssuesRepository.Update(TeamIssueMapper.MapToEntity(model));
            // Try to Save Changes
            TeamIssuesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = TeamIssuesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = TeamIssuesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ITeamIssue entity)
        {
            // Deactivate it
            TeamIssuesRepository.Deactivate(entity);
            // Try to Save Changes
            TeamIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = TeamIssuesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = TeamIssuesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ITeamIssue entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            TeamIssuesRepository.Remove(entity);
            // Try to Save Changes
            TeamIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
