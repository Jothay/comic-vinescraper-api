


// <auto-generated>
// <copyright file="Characters.CharacterEnemyBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the CharacterEnemy business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharacterEnemiesBusinessWorkflow : ICharacterEnemiesBusinessWorkflow
    {
        public CharacterEnemiesBusinessWorkflow(ICharacterEnemiesRepository characterEnemiesRepository, ICharacterEnemyMapper characterEnemyMapper)
        {
            CharacterEnemiesRepository = characterEnemiesRepository;
            CharacterEnemyMapper = characterEnemyMapper;
        }

        #region Private Variables
        private ICharacterEnemyMapper CharacterEnemyMapper { get; }
        private ICharacterEnemiesRepository CharacterEnemiesRepository { get; }
        #endregion
        #region Read
        public ICharacterEnemyModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterEnemyMapper.MapToModel(CharacterEnemiesRepository.Get(id));
        }

        public ICharacterEnemyModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterEnemyMapper.MapToModel(CharacterEnemiesRepository.Get(key));
        }

        public List<ICharacterEnemyModel> Search(ICharacterEnemySearchModel searchModel, bool asListing = false)
        {
            var results = CharacterEnemiesRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterEnemyMapper.MapToModelListing).ToList()
                : results.Select(CharacterEnemyMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterEnemyModel Create(ICharacterEnemyModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterEnemyMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterEnemyMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharacterEnemiesRepository.Add(newEntity);
            // Try to Save Changes
            CharacterEnemiesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterEnemyModel Update(ICharacterEnemyModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharacterEnemiesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterEnemyMapper.AreEqual(model, existingEntity))
            {
                return CharacterEnemyMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterEnemyMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharacterEnemiesRepository.Update(CharacterEnemyMapper.MapToEntity(model));
            // Try to Save Changes
            CharacterEnemiesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterEnemiesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterEnemiesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacterEnemy entity)
        {
            // Deactivate it
            CharacterEnemiesRepository.Deactivate(entity);
            // Try to Save Changes
            CharacterEnemiesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterEnemiesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterEnemiesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacterEnemy entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharacterEnemiesRepository.Remove(entity);
            // Try to Save Changes
            CharacterEnemiesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
