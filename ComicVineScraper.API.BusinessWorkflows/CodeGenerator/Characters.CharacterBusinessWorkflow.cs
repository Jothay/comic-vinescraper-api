// <auto-generated>
// <copyright file="Characters.CharacterBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the Character business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharactersBusinessWorkflow : ICharactersBusinessWorkflow
    {
        public CharactersBusinessWorkflow(ICharactersRepository charactersRepository, ICharacterMapper characterMapper)
        {
            CharactersRepository = charactersRepository;
            CharacterMapper = characterMapper;
        }

        #region Private Variables
        private ICharacterMapper CharacterMapper { get; }
        private ICharactersRepository CharactersRepository { get; }
        #endregion
        #region Read
        public ICharacterModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterMapper.MapToModel(CharactersRepository.Get(id));
        }

        public ICharacterModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterMapper.MapToModel(CharactersRepository.Get(key));
        }

        public List<ICharacterModel> Search(ICharacterSearchModel searchModel, bool asListing = false)
        {
            var results = CharactersRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterMapper.MapToModelListing).ToList()
                : results.Select(CharacterMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterModel Create(ICharacterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharactersRepository.Add(newEntity);
            // Try to Save Changes
            CharactersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterModel Update(ICharacterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharactersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterMapper.AreEqual(model, existingEntity))
            {
                return CharacterMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharactersRepository.Update(CharacterMapper.MapToEntity(model));
            // Try to Save Changes
            CharactersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharactersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharactersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacter entity)
        {
            // Deactivate it
            CharactersRepository.Deactivate(entity);
            // Try to Save Changes
            CharactersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharactersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharactersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacter entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharactersRepository.Remove(entity);
            // Try to Save Changes
            CharactersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
