// <auto-generated>
// <copyright file="Movies.MovieStoryArcBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the MovieStoryArc business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Movies
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class MovieStoryArcsBusinessWorkflow : IMovieStoryArcsBusinessWorkflow
    {
        public MovieStoryArcsBusinessWorkflow(IMovieStoryArcsRepository movieStoryArcsRepository, IMovieStoryArcMapper movieStoryArcMapper)
        {
            MovieStoryArcsRepository = movieStoryArcsRepository;
            MovieStoryArcMapper = movieStoryArcMapper;
        }

        #region Private Variables
        private IMovieStoryArcMapper MovieStoryArcMapper { get; }
        private IMovieStoryArcsRepository MovieStoryArcsRepository { get; }
        #endregion
        #region Read
        public IMovieStoryArcModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return MovieStoryArcMapper.MapToModel(MovieStoryArcsRepository.Get(id));
        }

        public IMovieStoryArcModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return MovieStoryArcMapper.MapToModel(MovieStoryArcsRepository.Get(key));
        }

        public List<IMovieStoryArcModel> Search(IMovieStoryArcSearchModel searchModel, bool asListing = false)
        {
            var results = MovieStoryArcsRepository.Search(searchModel);
            return asListing
                ? results.Select(MovieStoryArcMapper.MapToModelListing).ToList()
                : results.Select(MovieStoryArcMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IMovieStoryArcModel Create(IMovieStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(MovieStoryArcMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = MovieStoryArcMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            MovieStoryArcsRepository.Add(newEntity);
            // Try to Save Changes
            MovieStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IMovieStoryArcModel Update(IMovieStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = MovieStoryArcsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (MovieStoryArcMapper.AreEqual(model, existingEntity))
            {
                return MovieStoryArcMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            MovieStoryArcMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            MovieStoryArcsRepository.Update(MovieStoryArcMapper.MapToEntity(model));
            // Try to Save Changes
            MovieStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieStoryArcsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieStoryArcsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IMovieStoryArc entity)
        {
            // Deactivate it
            MovieStoryArcsRepository.Deactivate(entity);
            // Try to Save Changes
            MovieStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieStoryArcsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieStoryArcsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IMovieStoryArc entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            MovieStoryArcsRepository.Remove(entity);
            // Try to Save Changes
            MovieStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
