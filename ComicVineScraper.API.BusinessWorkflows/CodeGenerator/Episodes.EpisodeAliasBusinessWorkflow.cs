// <auto-generated>
// <copyright file="Episodes.EpisodeAliasBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the EpisodeAlias business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Episodes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class EpisodeAliasesBusinessWorkflow : IEpisodeAliasesBusinessWorkflow
    {
        public EpisodeAliasesBusinessWorkflow(IEpisodeAliasesRepository episodeAliasesRepository, IEpisodeAliasMapper episodeAliasMapper)
        {
            EpisodeAliasesRepository = episodeAliasesRepository;
            EpisodeAliasMapper = episodeAliasMapper;
        }

        #region Private Variables
        private IEpisodeAliasMapper EpisodeAliasMapper { get; }
        private IEpisodeAliasesRepository EpisodeAliasesRepository { get; }
        #endregion
        #region Read
        public IEpisodeAliasModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return EpisodeAliasMapper.MapToModel(EpisodeAliasesRepository.Get(id));
        }

        public IEpisodeAliasModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return EpisodeAliasMapper.MapToModel(EpisodeAliasesRepository.Get(key));
        }

        public List<IEpisodeAliasModel> Search(IEpisodeAliasSearchModel searchModel, bool asListing = false)
        {
            var results = EpisodeAliasesRepository.Search(searchModel);
            return asListing
                ? results.Select(EpisodeAliasMapper.MapToModelListing).ToList()
                : results.Select(EpisodeAliasMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IEpisodeAliasModel Create(IEpisodeAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(EpisodeAliasMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = EpisodeAliasMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            EpisodeAliasesRepository.Add(newEntity);
            // Try to Save Changes
            EpisodeAliasesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IEpisodeAliasModel Update(IEpisodeAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = EpisodeAliasesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (EpisodeAliasMapper.AreEqual(model, existingEntity))
            {
                return EpisodeAliasMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            EpisodeAliasMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            EpisodeAliasesRepository.Update(EpisodeAliasMapper.MapToEntity(model));
            // Try to Save Changes
            EpisodeAliasesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeAliasesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeAliasesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IEpisodeAlias entity)
        {
            // Deactivate it
            EpisodeAliasesRepository.Deactivate(entity);
            // Try to Save Changes
            EpisodeAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeAliasesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeAliasesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IEpisodeAlias entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            EpisodeAliasesRepository.Remove(entity);
            // Try to Save Changes
            EpisodeAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
