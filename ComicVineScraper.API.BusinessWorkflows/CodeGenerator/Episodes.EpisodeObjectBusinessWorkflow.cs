// <auto-generated>
// <copyright file="Episodes.EpisodeObjectBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the EpisodeObject business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Episodes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class EpisodeObjectsBusinessWorkflow : IEpisodeObjectsBusinessWorkflow
    {
        public EpisodeObjectsBusinessWorkflow(IEpisodeObjectsRepository episodeObjectsRepository, IEpisodeObjectMapper episodeObjectMapper)
        {
            EpisodeObjectsRepository = episodeObjectsRepository;
            EpisodeObjectMapper = episodeObjectMapper;
        }

        #region Private Variables
        private IEpisodeObjectMapper EpisodeObjectMapper { get; }
        private IEpisodeObjectsRepository EpisodeObjectsRepository { get; }
        #endregion
        #region Read
        public IEpisodeObjectModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return EpisodeObjectMapper.MapToModel(EpisodeObjectsRepository.Get(id));
        }

        public IEpisodeObjectModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return EpisodeObjectMapper.MapToModel(EpisodeObjectsRepository.Get(key));
        }

        public List<IEpisodeObjectModel> Search(IEpisodeObjectSearchModel searchModel, bool asListing = false)
        {
            var results = EpisodeObjectsRepository.Search(searchModel);
            return asListing
                ? results.Select(EpisodeObjectMapper.MapToModelListing).ToList()
                : results.Select(EpisodeObjectMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IEpisodeObjectModel Create(IEpisodeObjectModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(EpisodeObjectMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = EpisodeObjectMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            EpisodeObjectsRepository.Add(newEntity);
            // Try to Save Changes
            EpisodeObjectsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IEpisodeObjectModel Update(IEpisodeObjectModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = EpisodeObjectsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (EpisodeObjectMapper.AreEqual(model, existingEntity))
            {
                return EpisodeObjectMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            EpisodeObjectMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            EpisodeObjectsRepository.Update(EpisodeObjectMapper.MapToEntity(model));
            // Try to Save Changes
            EpisodeObjectsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeObjectsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeObjectsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IEpisodeObject entity)
        {
            // Deactivate it
            EpisodeObjectsRepository.Deactivate(entity);
            // Try to Save Changes
            EpisodeObjectsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeObjectsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeObjectsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IEpisodeObject entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            EpisodeObjectsRepository.Remove(entity);
            // Try to Save Changes
            EpisodeObjectsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
