// <auto-generated>
// <copyright file="Origins.IOriginProfileModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the OriginProfile model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Origins
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class OriginProfilesBusinessWorkflow : IOriginProfilesBusinessWorkflow
    {
        public OriginProfilesBusinessWorkflow(IOriginProfilesRepository originProfilesRepository, IOriginProfileMapper originProfileMapper)
        {
            OriginProfilesRepository = originProfilesRepository;
            OriginProfileMapper = originProfileMapper;
        }

        #region Private Variables
        private IOriginProfileMapper OriginProfileMapper { get; }
        private IOriginProfilesRepository OriginProfilesRepository { get; }
        #endregion
        #region Read
        public IOriginProfileModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return OriginProfileMapper.MapToModel(OriginProfilesRepository.Get(id));
        }

        public IOriginProfileModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return OriginProfileMapper.MapToModel(OriginProfilesRepository.Get(key));
        }

        public List<IOriginProfileModel> Search(IOriginProfileSearchModel searchModel, bool asListing = false)
        {
            var results = OriginProfilesRepository.Search(searchModel);
            return asListing
                ? results.Select(OriginProfileMapper.MapToModelListing).ToList()
                : results.Select(OriginProfileMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IOriginProfileModel Create(IOriginProfileModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(OriginProfileMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = OriginProfileMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            OriginProfilesRepository.Add(newEntity);
            // Try to Save Changes
            OriginProfilesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IOriginProfileModel Update(IOriginProfileModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = OriginProfilesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (OriginProfileMapper.AreEqual(model, existingEntity))
            {
                return OriginProfileMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            OriginProfileMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            OriginProfilesRepository.Update(OriginProfileMapper.MapToEntity(model));
            // Try to Save Changes
            OriginProfilesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = OriginProfilesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = OriginProfilesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IOriginProfile entity)
        {
            // Deactivate it
            OriginProfilesRepository.Deactivate(entity);
            // Try to Save Changes
            OriginProfilesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = OriginProfilesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = OriginProfilesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IOriginProfile entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            OriginProfilesRepository.Remove(entity);
            // Try to Save Changes
            OriginProfilesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
