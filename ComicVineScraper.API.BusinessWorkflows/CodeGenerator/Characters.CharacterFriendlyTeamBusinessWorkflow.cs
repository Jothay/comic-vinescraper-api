// <auto-generated>
// <copyright file="Characters.CharacterFriendlyTeamBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the CharacterFriendlyTeam business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharacterFriendlyTeamsBusinessWorkflow : ICharacterFriendlyTeamsBusinessWorkflow
    {
        public CharacterFriendlyTeamsBusinessWorkflow(ICharacterFriendlyTeamsRepository characterFriendlyTeamsRepository, ICharacterFriendlyTeamMapper characterFriendlyTeamMapper)
        {
            CharacterFriendlyTeamsRepository = characterFriendlyTeamsRepository;
            CharacterFriendlyTeamMapper = characterFriendlyTeamMapper;
        }

        #region Private Variables
        private ICharacterFriendlyTeamMapper CharacterFriendlyTeamMapper { get; }
        private ICharacterFriendlyTeamsRepository CharacterFriendlyTeamsRepository { get; }
        #endregion
        #region Read
        public ICharacterFriendlyTeamModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterFriendlyTeamMapper.MapToModel(CharacterFriendlyTeamsRepository.Get(id));
        }

        public ICharacterFriendlyTeamModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterFriendlyTeamMapper.MapToModel(CharacterFriendlyTeamsRepository.Get(key));
        }

        public List<ICharacterFriendlyTeamModel> Search(ICharacterFriendlyTeamSearchModel searchModel, bool asListing = false)
        {
            var results = CharacterFriendlyTeamsRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterFriendlyTeamMapper.MapToModelListing).ToList()
                : results.Select(CharacterFriendlyTeamMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterFriendlyTeamModel Create(ICharacterFriendlyTeamModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterFriendlyTeamMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterFriendlyTeamMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharacterFriendlyTeamsRepository.Add(newEntity);
            // Try to Save Changes
            CharacterFriendlyTeamsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterFriendlyTeamModel Update(ICharacterFriendlyTeamModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharacterFriendlyTeamsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterFriendlyTeamMapper.AreEqual(model, existingEntity))
            {
                return CharacterFriendlyTeamMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterFriendlyTeamMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharacterFriendlyTeamsRepository.Update(CharacterFriendlyTeamMapper.MapToEntity(model));
            // Try to Save Changes
            CharacterFriendlyTeamsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterFriendlyTeamsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterFriendlyTeamsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacterFriendlyTeam entity)
        {
            // Deactivate it
            CharacterFriendlyTeamsRepository.Deactivate(entity);
            // Try to Save Changes
            CharacterFriendlyTeamsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterFriendlyTeamsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterFriendlyTeamsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacterFriendlyTeam entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharacterFriendlyTeamsRepository.Remove(entity);
            // Try to Save Changes
            CharacterFriendlyTeamsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
