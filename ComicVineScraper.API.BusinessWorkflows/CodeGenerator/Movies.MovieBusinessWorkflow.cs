// <auto-generated>
// <copyright file="Movies.MovieBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the Movie business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Movies
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class MoviesBusinessWorkflow : IMoviesBusinessWorkflow
    {
        public MoviesBusinessWorkflow(IMoviesRepository moviesRepository, IMovieMapper movieMapper)
        {
            MoviesRepository = moviesRepository;
            MovieMapper = movieMapper;
        }

        #region Private Variables
        private IMovieMapper MovieMapper { get; }
        private IMoviesRepository MoviesRepository { get; }
        #endregion
        #region Read
        public IMovieModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return MovieMapper.MapToModel(MoviesRepository.Get(id));
        }

        public IMovieModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return MovieMapper.MapToModel(MoviesRepository.Get(key));
        }

        public List<IMovieModel> Search(IMovieSearchModel searchModel, bool asListing = false)
        {
            var results = MoviesRepository.Search(searchModel);
            return asListing
                ? results.Select(MovieMapper.MapToModelListing).ToList()
                : results.Select(MovieMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IMovieModel Create(IMovieModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(MovieMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = MovieMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            MoviesRepository.Add(newEntity);
            // Try to Save Changes
            MoviesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IMovieModel Update(IMovieModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = MoviesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (MovieMapper.AreEqual(model, existingEntity))
            {
                return MovieMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            MovieMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            MoviesRepository.Update(MovieMapper.MapToEntity(model));
            // Try to Save Changes
            MoviesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MoviesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MoviesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IMovie entity)
        {
            // Deactivate it
            MoviesRepository.Deactivate(entity);
            // Try to Save Changes
            MoviesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MoviesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MoviesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IMovie entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            MoviesRepository.Remove(entity);
            // Try to Save Changes
            MoviesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
