// <auto-generated>
// <copyright file="Movies.MovieObjectBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the MovieObject business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Movies
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class MovieObjectsBusinessWorkflow : IMovieObjectsBusinessWorkflow
    {
        public MovieObjectsBusinessWorkflow(IMovieObjectsRepository movieObjectsRepository, IMovieObjectMapper movieObjectMapper)
        {
            MovieObjectsRepository = movieObjectsRepository;
            MovieObjectMapper = movieObjectMapper;
        }

        #region Private Variables
        private IMovieObjectMapper MovieObjectMapper { get; }
        private IMovieObjectsRepository MovieObjectsRepository { get; }
        #endregion
        #region Read
        public IMovieObjectModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return MovieObjectMapper.MapToModel(MovieObjectsRepository.Get(id));
        }

        public IMovieObjectModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return MovieObjectMapper.MapToModel(MovieObjectsRepository.Get(key));
        }

        public List<IMovieObjectModel> Search(IMovieObjectSearchModel searchModel, bool asListing = false)
        {
            var results = MovieObjectsRepository.Search(searchModel);
            return asListing
                ? results.Select(MovieObjectMapper.MapToModelListing).ToList()
                : results.Select(MovieObjectMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IMovieObjectModel Create(IMovieObjectModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(MovieObjectMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = MovieObjectMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            MovieObjectsRepository.Add(newEntity);
            // Try to Save Changes
            MovieObjectsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IMovieObjectModel Update(IMovieObjectModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = MovieObjectsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (MovieObjectMapper.AreEqual(model, existingEntity))
            {
                return MovieObjectMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            MovieObjectMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            MovieObjectsRepository.Update(MovieObjectMapper.MapToEntity(model));
            // Try to Save Changes
            MovieObjectsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieObjectsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieObjectsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IMovieObject entity)
        {
            // Deactivate it
            MovieObjectsRepository.Deactivate(entity);
            // Try to Save Changes
            MovieObjectsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieObjectsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieObjectsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IMovieObject entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            MovieObjectsRepository.Remove(entity);
            // Try to Save Changes
            MovieObjectsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
