// <auto-generated>
// <copyright file="Locations.LocationAliasBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the LocationAlias business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Locations
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class LocationAliasesBusinessWorkflow : ILocationAliasesBusinessWorkflow
    {
        public LocationAliasesBusinessWorkflow(ILocationAliasesRepository locationAliasesRepository, ILocationAliasMapper locationAliasMapper)
        {
            LocationAliasesRepository = locationAliasesRepository;
            LocationAliasMapper = locationAliasMapper;
        }

        #region Private Variables
        private ILocationAliasMapper LocationAliasMapper { get; }
        private ILocationAliasesRepository LocationAliasesRepository { get; }
        #endregion
        #region Read
        public ILocationAliasModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return LocationAliasMapper.MapToModel(LocationAliasesRepository.Get(id));
        }

        public ILocationAliasModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return LocationAliasMapper.MapToModel(LocationAliasesRepository.Get(key));
        }

        public List<ILocationAliasModel> Search(ILocationAliasSearchModel searchModel, bool asListing = false)
        {
            var results = LocationAliasesRepository.Search(searchModel);
            return asListing
                ? results.Select(LocationAliasMapper.MapToModelListing).ToList()
                : results.Select(LocationAliasMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ILocationAliasModel Create(ILocationAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(LocationAliasMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = LocationAliasMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            LocationAliasesRepository.Add(newEntity);
            // Try to Save Changes
            LocationAliasesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ILocationAliasModel Update(ILocationAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = LocationAliasesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (LocationAliasMapper.AreEqual(model, existingEntity))
            {
                return LocationAliasMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            LocationAliasMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            LocationAliasesRepository.Update(LocationAliasMapper.MapToEntity(model));
            // Try to Save Changes
            LocationAliasesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationAliasesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationAliasesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ILocationAlias entity)
        {
            // Deactivate it
            LocationAliasesRepository.Deactivate(entity);
            // Try to Save Changes
            LocationAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationAliasesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationAliasesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ILocationAlias entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            LocationAliasesRepository.Remove(entity);
            // Try to Save Changes
            LocationAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
