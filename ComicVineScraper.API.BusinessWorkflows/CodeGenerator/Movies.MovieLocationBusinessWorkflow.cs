// <auto-generated>
// <copyright file="Movies.MovieLocationBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the MovieLocation business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Movies
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class MovieLocationsBusinessWorkflow : IMovieLocationsBusinessWorkflow
    {
        public MovieLocationsBusinessWorkflow(IMovieLocationsRepository movieLocationsRepository, IMovieLocationMapper movieLocationMapper)
        {
            MovieLocationsRepository = movieLocationsRepository;
            MovieLocationMapper = movieLocationMapper;
        }

        #region Private Variables
        private IMovieLocationMapper MovieLocationMapper { get; }
        private IMovieLocationsRepository MovieLocationsRepository { get; }
        #endregion
        #region Read
        public IMovieLocationModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return MovieLocationMapper.MapToModel(MovieLocationsRepository.Get(id));
        }

        public IMovieLocationModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return MovieLocationMapper.MapToModel(MovieLocationsRepository.Get(key));
        }

        public List<IMovieLocationModel> Search(IMovieLocationSearchModel searchModel, bool asListing = false)
        {
            var results = MovieLocationsRepository.Search(searchModel);
            return asListing
                ? results.Select(MovieLocationMapper.MapToModelListing).ToList()
                : results.Select(MovieLocationMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IMovieLocationModel Create(IMovieLocationModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(MovieLocationMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = MovieLocationMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            MovieLocationsRepository.Add(newEntity);
            // Try to Save Changes
            MovieLocationsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IMovieLocationModel Update(IMovieLocationModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = MovieLocationsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (MovieLocationMapper.AreEqual(model, existingEntity))
            {
                return MovieLocationMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            MovieLocationMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            MovieLocationsRepository.Update(MovieLocationMapper.MapToEntity(model));
            // Try to Save Changes
            MovieLocationsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieLocationsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieLocationsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IMovieLocation entity)
        {
            // Deactivate it
            MovieLocationsRepository.Deactivate(entity);
            // Try to Save Changes
            MovieLocationsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieLocationsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieLocationsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IMovieLocation entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            MovieLocationsRepository.Remove(entity);
            // Try to Save Changes
            MovieLocationsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
