// <auto-generated>
// <copyright file="Movies.IMovieProducerModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the MovieProducer model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Movies
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class MovieProducersBusinessWorkflow : IMovieProducersBusinessWorkflow
    {
        public MovieProducersBusinessWorkflow(IMovieProducersRepository movieProducersRepository, IMovieProducerMapper movieProducerMapper)
        {
            MovieProducersRepository = movieProducersRepository;
            MovieProducerMapper = movieProducerMapper;
        }

        #region Private Variables
        private IMovieProducerMapper MovieProducerMapper { get; }
        private IMovieProducersRepository MovieProducersRepository { get; }
        #endregion
        #region Read
        public IMovieProducerModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return MovieProducerMapper.MapToModel(MovieProducersRepository.Get(id));
        }

        public IMovieProducerModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return MovieProducerMapper.MapToModel(MovieProducersRepository.Get(key));
        }

        public List<IMovieProducerModel> Search(IMovieProducerSearchModel searchModel, bool asListing = false)
        {
            var results = MovieProducersRepository.Search(searchModel);
            return asListing
                ? results.Select(MovieProducerMapper.MapToModelListing).ToList()
                : results.Select(MovieProducerMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IMovieProducerModel Create(IMovieProducerModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(MovieProducerMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = MovieProducerMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            MovieProducersRepository.Add(newEntity);
            // Try to Save Changes
            MovieProducersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IMovieProducerModel Update(IMovieProducerModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = MovieProducersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (MovieProducerMapper.AreEqual(model, existingEntity))
            {
                return MovieProducerMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            MovieProducerMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            MovieProducersRepository.Update(MovieProducerMapper.MapToEntity(model));
            // Try to Save Changes
            MovieProducersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieProducersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieProducersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IMovieProducer entity)
        {
            // Deactivate it
            MovieProducersRepository.Deactivate(entity);
            // Try to Save Changes
            MovieProducersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieProducersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieProducersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IMovieProducer entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            MovieProducersRepository.Remove(entity);
            // Try to Save Changes
            MovieProducersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
