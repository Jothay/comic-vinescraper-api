// <auto-generated>
// <copyright file="Locations.LocationVolumeBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the LocationVolume business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Locations
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class LocationVolumesBusinessWorkflow : ILocationVolumesBusinessWorkflow
    {
        public LocationVolumesBusinessWorkflow(ILocationVolumesRepository locationVolumesRepository, ILocationVolumeMapper locationVolumeMapper)
        {
            LocationVolumesRepository = locationVolumesRepository;
            LocationVolumeMapper = locationVolumeMapper;
        }

        #region Private Variables
        private ILocationVolumeMapper LocationVolumeMapper { get; }
        private ILocationVolumesRepository LocationVolumesRepository { get; }
        #endregion
        #region Read
        public ILocationVolumeModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return LocationVolumeMapper.MapToModel(LocationVolumesRepository.Get(id));
        }

        public ILocationVolumeModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return LocationVolumeMapper.MapToModel(LocationVolumesRepository.Get(key));
        }

        public List<ILocationVolumeModel> Search(ILocationVolumeSearchModel searchModel, bool asListing = false)
        {
            var results = LocationVolumesRepository.Search(searchModel);
            return asListing
                ? results.Select(LocationVolumeMapper.MapToModelListing).ToList()
                : results.Select(LocationVolumeMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ILocationVolumeModel Create(ILocationVolumeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(LocationVolumeMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = LocationVolumeMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            LocationVolumesRepository.Add(newEntity);
            // Try to Save Changes
            LocationVolumesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ILocationVolumeModel Update(ILocationVolumeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = LocationVolumesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (LocationVolumeMapper.AreEqual(model, existingEntity))
            {
                return LocationVolumeMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            LocationVolumeMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            LocationVolumesRepository.Update(LocationVolumeMapper.MapToEntity(model));
            // Try to Save Changes
            LocationVolumesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationVolumesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationVolumesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ILocationVolume entity)
        {
            // Deactivate it
            LocationVolumesRepository.Deactivate(entity);
            // Try to Save Changes
            LocationVolumesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationVolumesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationVolumesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ILocationVolume entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            LocationVolumesRepository.Remove(entity);
            // Try to Save Changes
            LocationVolumesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
