// <auto-generated>
// <copyright file="Episodes.IEpisodeCharacterFirstAppearanceModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the EpisodeCharacterFirstAppearance model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Episodes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class EpisodeCharacterFirstAppearancesBusinessWorkflow : IEpisodeCharacterFirstAppearancesBusinessWorkflow
    {
        public EpisodeCharacterFirstAppearancesBusinessWorkflow(IEpisodeCharacterFirstAppearancesRepository episodeCharacterFirstAppearancesRepository, IEpisodeCharacterFirstAppearanceMapper episodeCharacterFirstAppearanceMapper)
        {
            EpisodeCharacterFirstAppearancesRepository = episodeCharacterFirstAppearancesRepository;
            EpisodeCharacterFirstAppearanceMapper = episodeCharacterFirstAppearanceMapper;
        }

        #region Private Variables
        private IEpisodeCharacterFirstAppearanceMapper EpisodeCharacterFirstAppearanceMapper { get; }
        private IEpisodeCharacterFirstAppearancesRepository EpisodeCharacterFirstAppearancesRepository { get; }
        #endregion
        #region Read
        public IEpisodeCharacterFirstAppearanceModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return EpisodeCharacterFirstAppearanceMapper.MapToModel(EpisodeCharacterFirstAppearancesRepository.Get(id));
        }

        public IEpisodeCharacterFirstAppearanceModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return EpisodeCharacterFirstAppearanceMapper.MapToModel(EpisodeCharacterFirstAppearancesRepository.Get(key));
        }

        public List<IEpisodeCharacterFirstAppearanceModel> Search(IEpisodeCharacterFirstAppearanceSearchModel searchModel, bool asListing = false)
        {
            var results = EpisodeCharacterFirstAppearancesRepository.Search(searchModel);
            return asListing
                ? results.Select(EpisodeCharacterFirstAppearanceMapper.MapToModelListing).ToList()
                : results.Select(EpisodeCharacterFirstAppearanceMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IEpisodeCharacterFirstAppearanceModel Create(IEpisodeCharacterFirstAppearanceModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(EpisodeCharacterFirstAppearanceMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = EpisodeCharacterFirstAppearanceMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            EpisodeCharacterFirstAppearancesRepository.Add(newEntity);
            // Try to Save Changes
            EpisodeCharacterFirstAppearancesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IEpisodeCharacterFirstAppearanceModel Update(IEpisodeCharacterFirstAppearanceModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = EpisodeCharacterFirstAppearancesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (EpisodeCharacterFirstAppearanceMapper.AreEqual(model, existingEntity))
            {
                return EpisodeCharacterFirstAppearanceMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            EpisodeCharacterFirstAppearanceMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            EpisodeCharacterFirstAppearancesRepository.Update(EpisodeCharacterFirstAppearanceMapper.MapToEntity(model));
            // Try to Save Changes
            EpisodeCharacterFirstAppearancesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeCharacterFirstAppearancesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeCharacterFirstAppearancesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IEpisodeCharacterFirstAppearance entity)
        {
            // Deactivate it
            EpisodeCharacterFirstAppearancesRepository.Deactivate(entity);
            // Try to Save Changes
            EpisodeCharacterFirstAppearancesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeCharacterFirstAppearancesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeCharacterFirstAppearancesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IEpisodeCharacterFirstAppearance entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            EpisodeCharacterFirstAppearancesRepository.Remove(entity);
            // Try to Save Changes
            EpisodeCharacterFirstAppearancesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
