// <auto-generated>
// <copyright file="Publishers.PublisherBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the Publisher business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Publishers
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class PublishersBusinessWorkflow : IPublishersBusinessWorkflow
    {
        public PublishersBusinessWorkflow(IPublishersRepository publishersRepository, IPublisherMapper publisherMapper)
        {
            PublishersRepository = publishersRepository;
            PublisherMapper = publisherMapper;
        }

        #region Private Variables
        private IPublisherMapper PublisherMapper { get; }
        private IPublishersRepository PublishersRepository { get; }
        #endregion
        #region Read
        public IPublisherModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return PublisherMapper.MapToModel(PublishersRepository.Get(id));
        }

        public IPublisherModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return PublisherMapper.MapToModel(PublishersRepository.Get(key));
        }

        public List<IPublisherModel> Search(IPublisherSearchModel searchModel, bool asListing = false)
        {
            var results = PublishersRepository.Search(searchModel);
            return asListing
                ? results.Select(PublisherMapper.MapToModelListing).ToList()
                : results.Select(PublisherMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IPublisherModel Create(IPublisherModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(PublisherMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = PublisherMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            PublishersRepository.Add(newEntity);
            // Try to Save Changes
            PublishersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IPublisherModel Update(IPublisherModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = PublishersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (PublisherMapper.AreEqual(model, existingEntity))
            {
                return PublisherMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            PublisherMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            PublishersRepository.Update(PublisherMapper.MapToEntity(model));
            // Try to Save Changes
            PublishersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = PublishersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = PublishersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IPublisher entity)
        {
            // Deactivate it
            PublishersRepository.Deactivate(entity);
            // Try to Save Changes
            PublishersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = PublishersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = PublishersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IPublisher entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            PublishersRepository.Remove(entity);
            // Try to Save Changes
            PublishersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
