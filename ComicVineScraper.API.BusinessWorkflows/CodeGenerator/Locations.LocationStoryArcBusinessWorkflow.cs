// <auto-generated>
// <copyright file="Locations.LocationStoryArcBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the LocationStoryArc business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Locations
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class LocationStoryArcsBusinessWorkflow : ILocationStoryArcsBusinessWorkflow
    {
        public LocationStoryArcsBusinessWorkflow(ILocationStoryArcsRepository locationStoryArcsRepository, ILocationStoryArcMapper locationStoryArcMapper)
        {
            LocationStoryArcsRepository = locationStoryArcsRepository;
            LocationStoryArcMapper = locationStoryArcMapper;
        }

        #region Private Variables
        private ILocationStoryArcMapper LocationStoryArcMapper { get; }
        private ILocationStoryArcsRepository LocationStoryArcsRepository { get; }
        #endregion
        #region Read
        public ILocationStoryArcModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return LocationStoryArcMapper.MapToModel(LocationStoryArcsRepository.Get(id));
        }

        public ILocationStoryArcModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return LocationStoryArcMapper.MapToModel(LocationStoryArcsRepository.Get(key));
        }

        public List<ILocationStoryArcModel> Search(ILocationStoryArcSearchModel searchModel, bool asListing = false)
        {
            var results = LocationStoryArcsRepository.Search(searchModel);
            return asListing
                ? results.Select(LocationStoryArcMapper.MapToModelListing).ToList()
                : results.Select(LocationStoryArcMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ILocationStoryArcModel Create(ILocationStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(LocationStoryArcMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = LocationStoryArcMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            LocationStoryArcsRepository.Add(newEntity);
            // Try to Save Changes
            LocationStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ILocationStoryArcModel Update(ILocationStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = LocationStoryArcsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (LocationStoryArcMapper.AreEqual(model, existingEntity))
            {
                return LocationStoryArcMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            LocationStoryArcMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            LocationStoryArcsRepository.Update(LocationStoryArcMapper.MapToEntity(model));
            // Try to Save Changes
            LocationStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationStoryArcsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationStoryArcsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ILocationStoryArc entity)
        {
            // Deactivate it
            LocationStoryArcsRepository.Deactivate(entity);
            // Try to Save Changes
            LocationStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationStoryArcsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationStoryArcsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ILocationStoryArc entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            LocationStoryArcsRepository.Remove(entity);
            // Try to Save Changes
            LocationStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
