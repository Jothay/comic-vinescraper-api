// <auto-generated>
// <copyright file="Characters.CharacterAliasBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the CharacterAlias business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharacterAliasesBusinessWorkflow : ICharacterAliasesBusinessWorkflow
    {
        public CharacterAliasesBusinessWorkflow(ICharacterAliasesRepository characterAliasesRepository, ICharacterAliasMapper characterAliasMapper)
        {
            CharacterAliasesRepository = characterAliasesRepository;
            CharacterAliasMapper = characterAliasMapper;
        }

        #region Private Variables
        private ICharacterAliasMapper CharacterAliasMapper { get; }
        private ICharacterAliasesRepository CharacterAliasesRepository { get; }
        #endregion
        #region Read
        public ICharacterAliasModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterAliasMapper.MapToModel(CharacterAliasesRepository.Get(id));
        }

        public ICharacterAliasModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterAliasMapper.MapToModel(CharacterAliasesRepository.Get(key));
        }

        public List<ICharacterAliasModel> Search(ICharacterAliasSearchModel searchModel, bool asListing = false)
        {
            var results = CharacterAliasesRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterAliasMapper.MapToModelListing).ToList()
                : results.Select(CharacterAliasMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterAliasModel Create(ICharacterAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterAliasMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterAliasMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharacterAliasesRepository.Add(newEntity);
            // Try to Save Changes
            CharacterAliasesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterAliasModel Update(ICharacterAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharacterAliasesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterAliasMapper.AreEqual(model, existingEntity))
            {
                return CharacterAliasMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterAliasMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharacterAliasesRepository.Update(CharacterAliasMapper.MapToEntity(model));
            // Try to Save Changes
            CharacterAliasesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterAliasesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterAliasesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacterAlias entity)
        {
            // Deactivate it
            CharacterAliasesRepository.Deactivate(entity);
            // Try to Save Changes
            CharacterAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterAliasesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterAliasesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacterAlias entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharacterAliasesRepository.Remove(entity);
            // Try to Save Changes
            CharacterAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
