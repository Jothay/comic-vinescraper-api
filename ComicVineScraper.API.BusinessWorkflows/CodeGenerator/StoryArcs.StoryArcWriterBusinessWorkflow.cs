// <auto-generated>
// <copyright file="StoryArcs.StoryArcWriterBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the StoryArcWriter business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.StoryArcs
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class StoryArcWritersBusinessWorkflow : IStoryArcWritersBusinessWorkflow
    {
        public StoryArcWritersBusinessWorkflow(IStoryArcWritersRepository storyArcWritersRepository, IStoryArcWriterMapper storyArcWriterMapper)
        {
            StoryArcWritersRepository = storyArcWritersRepository;
            StoryArcWriterMapper = storyArcWriterMapper;
        }

        #region Private Variables
        private IStoryArcWriterMapper StoryArcWriterMapper { get; }
        private IStoryArcWritersRepository StoryArcWritersRepository { get; }
        #endregion
        #region Read
        public IStoryArcWriterModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return StoryArcWriterMapper.MapToModel(StoryArcWritersRepository.Get(id));
        }

        public IStoryArcWriterModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return StoryArcWriterMapper.MapToModel(StoryArcWritersRepository.Get(key));
        }

        public List<IStoryArcWriterModel> Search(IStoryArcWriterSearchModel searchModel, bool asListing = false)
        {
            var results = StoryArcWritersRepository.Search(searchModel);
            return asListing
                ? results.Select(StoryArcWriterMapper.MapToModelListing).ToList()
                : results.Select(StoryArcWriterMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IStoryArcWriterModel Create(IStoryArcWriterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(StoryArcWriterMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = StoryArcWriterMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            StoryArcWritersRepository.Add(newEntity);
            // Try to Save Changes
            StoryArcWritersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IStoryArcWriterModel Update(IStoryArcWriterModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = StoryArcWritersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (StoryArcWriterMapper.AreEqual(model, existingEntity))
            {
                return StoryArcWriterMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            StoryArcWriterMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            StoryArcWritersRepository.Update(StoryArcWriterMapper.MapToEntity(model));
            // Try to Save Changes
            StoryArcWritersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = StoryArcWritersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = StoryArcWritersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IStoryArcWriter entity)
        {
            // Deactivate it
            StoryArcWritersRepository.Deactivate(entity);
            // Try to Save Changes
            StoryArcWritersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = StoryArcWritersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = StoryArcWritersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IStoryArcWriter entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            StoryArcWritersRepository.Remove(entity);
            // Try to Save Changes
            StoryArcWritersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
