// <auto-generated>
// <copyright file="Teams.TeamAppearedInIssueBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the TeamAppearedInIssue business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Teams
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class TeamAppearedInIssuesBusinessWorkflow : ITeamAppearedInIssuesBusinessWorkflow
    {
        public TeamAppearedInIssuesBusinessWorkflow(ITeamAppearedInIssuesRepository teamAppearedInIssuesRepository, ITeamAppearedInIssueMapper teamAppearedInIssueMapper)
        {
            TeamAppearedInIssuesRepository = teamAppearedInIssuesRepository;
            TeamAppearedInIssueMapper = teamAppearedInIssueMapper;
        }

        #region Private Variables
        private ITeamAppearedInIssueMapper TeamAppearedInIssueMapper { get; }
        private ITeamAppearedInIssuesRepository TeamAppearedInIssuesRepository { get; }
        #endregion
        #region Read
        public ITeamAppearedInIssueModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return TeamAppearedInIssueMapper.MapToModel(TeamAppearedInIssuesRepository.Get(id));
        }

        public ITeamAppearedInIssueModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return TeamAppearedInIssueMapper.MapToModel(TeamAppearedInIssuesRepository.Get(key));
        }

        public List<ITeamAppearedInIssueModel> Search(ITeamAppearedInIssueSearchModel searchModel, bool asListing = false)
        {
            var results = TeamAppearedInIssuesRepository.Search(searchModel);
            return asListing
                ? results.Select(TeamAppearedInIssueMapper.MapToModelListing).ToList()
                : results.Select(TeamAppearedInIssueMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ITeamAppearedInIssueModel Create(ITeamAppearedInIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(TeamAppearedInIssueMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = TeamAppearedInIssueMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            TeamAppearedInIssuesRepository.Add(newEntity);
            // Try to Save Changes
            TeamAppearedInIssuesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ITeamAppearedInIssueModel Update(ITeamAppearedInIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = TeamAppearedInIssuesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (TeamAppearedInIssueMapper.AreEqual(model, existingEntity))
            {
                return TeamAppearedInIssueMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            TeamAppearedInIssueMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            TeamAppearedInIssuesRepository.Update(TeamAppearedInIssueMapper.MapToEntity(model));
            // Try to Save Changes
            TeamAppearedInIssuesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = TeamAppearedInIssuesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = TeamAppearedInIssuesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ITeamAppearedInIssue entity)
        {
            // Deactivate it
            TeamAppearedInIssuesRepository.Deactivate(entity);
            // Try to Save Changes
            TeamAppearedInIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = TeamAppearedInIssuesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = TeamAppearedInIssuesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ITeamAppearedInIssue entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            TeamAppearedInIssuesRepository.Remove(entity);
            // Try to Save Changes
            TeamAppearedInIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
