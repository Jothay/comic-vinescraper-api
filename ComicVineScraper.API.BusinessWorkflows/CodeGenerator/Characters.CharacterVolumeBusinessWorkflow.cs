// <auto-generated>
// <copyright file="Characters.CharacterVolumeBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the CharacterVolume business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharacterVolumesBusinessWorkflow : ICharacterVolumesBusinessWorkflow
    {
        public CharacterVolumesBusinessWorkflow(ICharacterVolumesRepository characterVolumesRepository, ICharacterVolumeMapper characterVolumeMapper)
        {
            CharacterVolumesRepository = characterVolumesRepository;
            CharacterVolumeMapper = characterVolumeMapper;
        }

        #region Private Variables
        private ICharacterVolumeMapper CharacterVolumeMapper { get; }
        private ICharacterVolumesRepository CharacterVolumesRepository { get; }
        #endregion
        #region Read
        public ICharacterVolumeModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterVolumeMapper.MapToModel(CharacterVolumesRepository.Get(id));
        }

        public ICharacterVolumeModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterVolumeMapper.MapToModel(CharacterVolumesRepository.Get(key));
        }

        public List<ICharacterVolumeModel> Search(ICharacterVolumeSearchModel searchModel, bool asListing = false)
        {
            var results = CharacterVolumesRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterVolumeMapper.MapToModelListing).ToList()
                : results.Select(CharacterVolumeMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterVolumeModel Create(ICharacterVolumeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterVolumeMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterVolumeMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharacterVolumesRepository.Add(newEntity);
            // Try to Save Changes
            CharacterVolumesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterVolumeModel Update(ICharacterVolumeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharacterVolumesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterVolumeMapper.AreEqual(model, existingEntity))
            {
                return CharacterVolumeMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterVolumeMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharacterVolumesRepository.Update(CharacterVolumeMapper.MapToEntity(model));
            // Try to Save Changes
            CharacterVolumesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterVolumesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterVolumesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacterVolume entity)
        {
            // Deactivate it
            CharacterVolumesRepository.Deactivate(entity);
            // Try to Save Changes
            CharacterVolumesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterVolumesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterVolumesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacterVolume entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharacterVolumesRepository.Remove(entity);
            // Try to Save Changes
            CharacterVolumesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
