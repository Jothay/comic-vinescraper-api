// <auto-generated>
// <copyright file="Series.SeriesBusinessWorkflow.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the Series business workflows.</summary>
// <remarks>This file was auto-generated using WorkflowFileMaster.tt in
// ComicVineScraper.API.BusinessWorkflows\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.BusinessWorkflows//.Series
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class SeriesBusinessWorkflow : ISeriesBusinessWorkflow
    {
        public SeriesBusinessWorkflow(ISeriesRepository seriesRepository, ISeriesMapper seriesMapper)
        {
            SeriesRepository = seriesRepository;
            SeriesMapper = seriesMapper;
        }

        #region Private Variables
        private ISeriesMapper SeriesMapper { get; }
        private ISeriesRepository SeriesRepository { get; }
        #endregion
        #region Read
        public ISeriesModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return SeriesMapper.MapToModel(SeriesRepository.Get(id));
        }

        public ISeriesModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return SeriesMapper.MapToModel(SeriesRepository.Get(key));
        }

        public List<ISeriesModel> Search(ISeriesSearchModel searchModel, bool asListing = false)
        {
            var results = SeriesRepository.Search(searchModel);
            return asListing
                ? results.Select(SeriesMapper.MapToModelListing).ToList()
                : results.Select(SeriesMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ISeriesModel Create(ISeriesModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(SeriesMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = SeriesMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            SeriesRepository.Add(newEntity);
            // Try to Save Changes
            SeriesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ISeriesModel Update(ISeriesModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = SeriesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (SeriesMapper.AreEqual(model, existingEntity))
            {
                return SeriesMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            SeriesMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            SeriesRepository.Update(SeriesMapper.MapToEntity(model));
            // Try to Save Changes
            SeriesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = SeriesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = SeriesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ISeries entity)
        {
            // Deactivate it
            SeriesRepository.Deactivate(entity);
            // Try to Save Changes
            SeriesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = SeriesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = SeriesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ISeries entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            SeriesRepository.Remove(entity);
            // Try to Save Changes
            SeriesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
