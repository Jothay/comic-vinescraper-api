// <auto-generated>
// <copyright file="Characters.ICharacterDiedInIssueModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the CharacterDiedInIssue model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharacterDiedInIssuesBusinessWorkflow : ICharacterDiedInIssuesBusinessWorkflow
    {
        public CharacterDiedInIssuesBusinessWorkflow(ICharacterDiedInIssuesRepository characterDiedInIssuesRepository, ICharacterDiedInIssueMapper characterDiedInIssueMapper)
        {
            CharacterDiedInIssuesRepository = characterDiedInIssuesRepository;
            CharacterDiedInIssueMapper = characterDiedInIssueMapper;
        }

        #region Private Variables
        private ICharacterDiedInIssueMapper CharacterDiedInIssueMapper { get; }
        private ICharacterDiedInIssuesRepository CharacterDiedInIssuesRepository { get; }
        #endregion
        #region Read
        public ICharacterDiedInIssueModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterDiedInIssueMapper.MapToModel(CharacterDiedInIssuesRepository.Get(id));
        }

        public ICharacterDiedInIssueModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterDiedInIssueMapper.MapToModel(CharacterDiedInIssuesRepository.Get(key));
        }

        public List<ICharacterDiedInIssueModel> Search(ICharacterDiedInIssueSearchModel searchModel, bool asListing = false)
        {
            var results = CharacterDiedInIssuesRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterDiedInIssueMapper.MapToModelListing).ToList()
                : results.Select(CharacterDiedInIssueMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterDiedInIssueModel Create(ICharacterDiedInIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterDiedInIssueMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterDiedInIssueMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharacterDiedInIssuesRepository.Add(newEntity);
            // Try to Save Changes
            CharacterDiedInIssuesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterDiedInIssueModel Update(ICharacterDiedInIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharacterDiedInIssuesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterDiedInIssueMapper.AreEqual(model, existingEntity))
            {
                return CharacterDiedInIssueMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterDiedInIssueMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharacterDiedInIssuesRepository.Update(CharacterDiedInIssueMapper.MapToEntity(model));
            // Try to Save Changes
            CharacterDiedInIssuesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterDiedInIssuesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterDiedInIssuesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacterDiedInIssue entity)
        {
            // Deactivate it
            CharacterDiedInIssuesRepository.Deactivate(entity);
            // Try to Save Changes
            CharacterDiedInIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterDiedInIssuesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterDiedInIssuesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacterDiedInIssue entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharacterDiedInIssuesRepository.Remove(entity);
            // Try to Save Changes
            CharacterDiedInIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
