// <auto-generated>
// <copyright file="Concepts.IConceptMovieModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the ConceptMovie model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Concepts
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ConceptMoviesBusinessWorkflow : IConceptMoviesBusinessWorkflow
    {
        public ConceptMoviesBusinessWorkflow(IConceptMoviesRepository conceptMoviesRepository, IConceptMovieMapper conceptMovieMapper)
        {
            ConceptMoviesRepository = conceptMoviesRepository;
            ConceptMovieMapper = conceptMovieMapper;
        }

        #region Private Variables
        private IConceptMovieMapper ConceptMovieMapper { get; }
        private IConceptMoviesRepository ConceptMoviesRepository { get; }
        #endregion
        #region Read
        public IConceptMovieModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ConceptMovieMapper.MapToModel(ConceptMoviesRepository.Get(id));
        }

        public IConceptMovieModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ConceptMovieMapper.MapToModel(ConceptMoviesRepository.Get(key));
        }

        public List<IConceptMovieModel> Search(IConceptMovieSearchModel searchModel, bool asListing = false)
        {
            var results = ConceptMoviesRepository.Search(searchModel);
            return asListing
                ? results.Select(ConceptMovieMapper.MapToModelListing).ToList()
                : results.Select(ConceptMovieMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IConceptMovieModel Create(IConceptMovieModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ConceptMovieMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ConceptMovieMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ConceptMoviesRepository.Add(newEntity);
            // Try to Save Changes
            ConceptMoviesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IConceptMovieModel Update(IConceptMovieModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ConceptMoviesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ConceptMovieMapper.AreEqual(model, existingEntity))
            {
                return ConceptMovieMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ConceptMovieMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ConceptMoviesRepository.Update(ConceptMovieMapper.MapToEntity(model));
            // Try to Save Changes
            ConceptMoviesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ConceptMoviesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ConceptMoviesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IConceptMovie entity)
        {
            // Deactivate it
            ConceptMoviesRepository.Deactivate(entity);
            // Try to Save Changes
            ConceptMoviesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ConceptMoviesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ConceptMoviesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IConceptMovie entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ConceptMoviesRepository.Remove(entity);
            // Try to Save Changes
            ConceptMoviesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
