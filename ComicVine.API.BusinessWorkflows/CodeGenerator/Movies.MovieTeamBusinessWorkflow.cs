// <auto-generated>
// <copyright file="Movies.IMovieTeamModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the MovieTeam model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Movies
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class MovieTeamsBusinessWorkflow : IMovieTeamsBusinessWorkflow
    {
        public MovieTeamsBusinessWorkflow(IMovieTeamsRepository movieTeamsRepository, IMovieTeamMapper movieTeamMapper)
        {
            MovieTeamsRepository = movieTeamsRepository;
            MovieTeamMapper = movieTeamMapper;
        }

        #region Private Variables
        private IMovieTeamMapper MovieTeamMapper { get; }
        private IMovieTeamsRepository MovieTeamsRepository { get; }
        #endregion
        #region Read
        public IMovieTeamModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return MovieTeamMapper.MapToModel(MovieTeamsRepository.Get(id));
        }

        public IMovieTeamModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return MovieTeamMapper.MapToModel(MovieTeamsRepository.Get(key));
        }

        public List<IMovieTeamModel> Search(IMovieTeamSearchModel searchModel, bool asListing = false)
        {
            var results = MovieTeamsRepository.Search(searchModel);
            return asListing
                ? results.Select(MovieTeamMapper.MapToModelListing).ToList()
                : results.Select(MovieTeamMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IMovieTeamModel Create(IMovieTeamModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(MovieTeamMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = MovieTeamMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            MovieTeamsRepository.Add(newEntity);
            // Try to Save Changes
            MovieTeamsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IMovieTeamModel Update(IMovieTeamModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = MovieTeamsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (MovieTeamMapper.AreEqual(model, existingEntity))
            {
                return MovieTeamMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            MovieTeamMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            MovieTeamsRepository.Update(MovieTeamMapper.MapToEntity(model));
            // Try to Save Changes
            MovieTeamsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieTeamsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieTeamsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IMovieTeam entity)
        {
            // Deactivate it
            MovieTeamsRepository.Deactivate(entity);
            // Try to Save Changes
            MovieTeamsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = MovieTeamsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = MovieTeamsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IMovieTeam entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            MovieTeamsRepository.Remove(entity);
            // Try to Save Changes
            MovieTeamsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
