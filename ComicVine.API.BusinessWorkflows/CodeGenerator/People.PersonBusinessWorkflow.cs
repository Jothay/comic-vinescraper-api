// <auto-generated>
// <copyright file="People.IPersonModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the Person model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.People
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class PeopleBusinessWorkflow : IPeopleBusinessWorkflow
    {
        public PeopleBusinessWorkflow(IPeopleRepository peopleRepository, IPersonMapper personMapper)
        {
            PeopleRepository = peopleRepository;
            PersonMapper = personMapper;
        }

        #region Private Variables
        private IPersonMapper PersonMapper { get; }
        private IPeopleRepository PeopleRepository { get; }
        #endregion
        #region Read
        public IPersonModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return PersonMapper.MapToModel(PeopleRepository.Get(id));
        }

        public IPersonModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return PersonMapper.MapToModel(PeopleRepository.Get(key));
        }

        public List<IPersonModel> Search(IPersonSearchModel searchModel, bool asListing = false)
        {
            var results = PeopleRepository.Search(searchModel);
            return asListing
                ? results.Select(PersonMapper.MapToModelListing).ToList()
                : results.Select(PersonMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IPersonModel Create(IPersonModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(PersonMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = PersonMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            PeopleRepository.Add(newEntity);
            // Try to Save Changes
            PeopleRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IPersonModel Update(IPersonModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = PeopleRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (PersonMapper.AreEqual(model, existingEntity))
            {
                return PersonMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            PersonMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            PeopleRepository.Update(PersonMapper.MapToEntity(model));
            // Try to Save Changes
            PeopleRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = PeopleRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = PeopleRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IPerson entity)
        {
            // Deactivate it
            PeopleRepository.Deactivate(entity);
            // Try to Save Changes
            PeopleRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = PeopleRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = PeopleRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IPerson entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            PeopleRepository.Remove(entity);
            // Try to Save Changes
            PeopleRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
