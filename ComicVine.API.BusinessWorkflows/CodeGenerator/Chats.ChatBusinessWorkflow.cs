// <auto-generated>
// <copyright file="Chats.IChatModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the Chat model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Chats
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ChatsBusinessWorkflow : IChatsBusinessWorkflow
    {
        public ChatsBusinessWorkflow(IChatsRepository chatsRepository, IChatMapper chatMapper)
        {
            ChatsRepository = chatsRepository;
            ChatMapper = chatMapper;
        }

        #region Private Variables
        private IChatMapper ChatMapper { get; }
        private IChatsRepository ChatsRepository { get; }
        #endregion
        #region Read
        public IChatModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ChatMapper.MapToModel(ChatsRepository.Get(id));
        }

        public IChatModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ChatMapper.MapToModel(ChatsRepository.Get(key));
        }

        public List<IChatModel> Search(IChatSearchModel searchModel, bool asListing = false)
        {
            var results = ChatsRepository.Search(searchModel);
            return asListing
                ? results.Select(ChatMapper.MapToModelListing).ToList()
                : results.Select(ChatMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IChatModel Create(IChatModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ChatMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ChatMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ChatsRepository.Add(newEntity);
            // Try to Save Changes
            ChatsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IChatModel Update(IChatModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ChatsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ChatMapper.AreEqual(model, existingEntity))
            {
                return ChatMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ChatMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ChatsRepository.Update(ChatMapper.MapToEntity(model));
            // Try to Save Changes
            ChatsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ChatsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ChatsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IChat entity)
        {
            // Deactivate it
            ChatsRepository.Deactivate(entity);
            // Try to Save Changes
            ChatsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ChatsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ChatsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IChat entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ChatsRepository.Remove(entity);
            // Try to Save Changes
            ChatsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
