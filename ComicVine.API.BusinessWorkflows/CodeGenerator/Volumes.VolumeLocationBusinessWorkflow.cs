// <auto-generated>
// <copyright file="Volumes.IVolumeLocationModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the VolumeLocation model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Volumes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class VolumeLocationsBusinessWorkflow : IVolumeLocationsBusinessWorkflow
    {
        public VolumeLocationsBusinessWorkflow(IVolumeLocationsRepository volumeLocationsRepository, IVolumeLocationMapper volumeLocationMapper)
        {
            VolumeLocationsRepository = volumeLocationsRepository;
            VolumeLocationMapper = volumeLocationMapper;
        }

        #region Private Variables
        private IVolumeLocationMapper VolumeLocationMapper { get; }
        private IVolumeLocationsRepository VolumeLocationsRepository { get; }
        #endregion
        #region Read
        public IVolumeLocationModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return VolumeLocationMapper.MapToModel(VolumeLocationsRepository.Get(id));
        }

        public IVolumeLocationModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return VolumeLocationMapper.MapToModel(VolumeLocationsRepository.Get(key));
        }

        public List<IVolumeLocationModel> Search(IVolumeLocationSearchModel searchModel, bool asListing = false)
        {
            var results = VolumeLocationsRepository.Search(searchModel);
            return asListing
                ? results.Select(VolumeLocationMapper.MapToModelListing).ToList()
                : results.Select(VolumeLocationMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IVolumeLocationModel Create(IVolumeLocationModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(VolumeLocationMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = VolumeLocationMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            VolumeLocationsRepository.Add(newEntity);
            // Try to Save Changes
            VolumeLocationsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IVolumeLocationModel Update(IVolumeLocationModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = VolumeLocationsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (VolumeLocationMapper.AreEqual(model, existingEntity))
            {
                return VolumeLocationMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            VolumeLocationMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            VolumeLocationsRepository.Update(VolumeLocationMapper.MapToEntity(model));
            // Try to Save Changes
            VolumeLocationsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumeLocationsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumeLocationsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IVolumeLocation entity)
        {
            // Deactivate it
            VolumeLocationsRepository.Deactivate(entity);
            // Try to Save Changes
            VolumeLocationsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VolumeLocationsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VolumeLocationsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IVolumeLocation entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            VolumeLocationsRepository.Remove(entity);
            // Try to Save Changes
            VolumeLocationsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
