// <auto-generated>
// <copyright file="Powers.IPowerModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the Power model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Powers
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class PowersBusinessWorkflow : IPowersBusinessWorkflow
    {
        public PowersBusinessWorkflow(IPowersRepository powersRepository, IPowerMapper powerMapper)
        {
            PowersRepository = powersRepository;
            PowerMapper = powerMapper;
        }

        #region Private Variables
        private IPowerMapper PowerMapper { get; }
        private IPowersRepository PowersRepository { get; }
        #endregion
        #region Read
        public IPowerModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return PowerMapper.MapToModel(PowersRepository.Get(id));
        }

        public IPowerModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return PowerMapper.MapToModel(PowersRepository.Get(key));
        }

        public List<IPowerModel> Search(IPowerSearchModel searchModel, bool asListing = false)
        {
            var results = PowersRepository.Search(searchModel);
            return asListing
                ? results.Select(PowerMapper.MapToModelListing).ToList()
                : results.Select(PowerMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IPowerModel Create(IPowerModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(PowerMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = PowerMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            PowersRepository.Add(newEntity);
            // Try to Save Changes
            PowersRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IPowerModel Update(IPowerModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = PowersRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (PowerMapper.AreEqual(model, existingEntity))
            {
                return PowerMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            PowerMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            PowersRepository.Update(PowerMapper.MapToEntity(model));
            // Try to Save Changes
            PowersRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = PowersRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = PowersRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IPower entity)
        {
            // Deactivate it
            PowersRepository.Deactivate(entity);
            // Try to Save Changes
            PowersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = PowersRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = PowersRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IPower entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            PowersRepository.Remove(entity);
            // Try to Save Changes
            PowersRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
