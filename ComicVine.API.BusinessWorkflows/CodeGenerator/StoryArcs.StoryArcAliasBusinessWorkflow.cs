// <auto-generated>
// <copyright file="StoryArcs.IStoryArcAliasModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the StoryArcAlias model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.StoryArcs
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class StoryArcAliasesBusinessWorkflow : IStoryArcAliasesBusinessWorkflow
    {
        public StoryArcAliasesBusinessWorkflow(IStoryArcAliasesRepository storyArcAliasesRepository, IStoryArcAliasMapper storyArcAliasMapper)
        {
            StoryArcAliasesRepository = storyArcAliasesRepository;
            StoryArcAliasMapper = storyArcAliasMapper;
        }

        #region Private Variables
        private IStoryArcAliasMapper StoryArcAliasMapper { get; }
        private IStoryArcAliasesRepository StoryArcAliasesRepository { get; }
        #endregion
        #region Read
        public IStoryArcAliasModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return StoryArcAliasMapper.MapToModel(StoryArcAliasesRepository.Get(id));
        }

        public IStoryArcAliasModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return StoryArcAliasMapper.MapToModel(StoryArcAliasesRepository.Get(key));
        }

        public List<IStoryArcAliasModel> Search(IStoryArcAliasSearchModel searchModel, bool asListing = false)
        {
            var results = StoryArcAliasesRepository.Search(searchModel);
            return asListing
                ? results.Select(StoryArcAliasMapper.MapToModelListing).ToList()
                : results.Select(StoryArcAliasMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IStoryArcAliasModel Create(IStoryArcAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(StoryArcAliasMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = StoryArcAliasMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            StoryArcAliasesRepository.Add(newEntity);
            // Try to Save Changes
            StoryArcAliasesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IStoryArcAliasModel Update(IStoryArcAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = StoryArcAliasesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (StoryArcAliasMapper.AreEqual(model, existingEntity))
            {
                return StoryArcAliasMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            StoryArcAliasMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            StoryArcAliasesRepository.Update(StoryArcAliasMapper.MapToEntity(model));
            // Try to Save Changes
            StoryArcAliasesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = StoryArcAliasesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = StoryArcAliasesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IStoryArcAlias entity)
        {
            // Deactivate it
            StoryArcAliasesRepository.Deactivate(entity);
            // Try to Save Changes
            StoryArcAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = StoryArcAliasesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = StoryArcAliasesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IStoryArcAlias entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            StoryArcAliasesRepository.Remove(entity);
            // Try to Save Changes
            StoryArcAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
