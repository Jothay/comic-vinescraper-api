// <auto-generated>
// <copyright file="Videos.IVideoTypeModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the VideoType model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Videos
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class VideoTypesBusinessWorkflow : IVideoTypesBusinessWorkflow
    {
        public VideoTypesBusinessWorkflow(IVideoTypesRepository videoTypesRepository, IVideoTypeMapper videoTypeMapper)
        {
            VideoTypesRepository = videoTypesRepository;
            VideoTypeMapper = videoTypeMapper;
        }

        #region Private Variables
        private IVideoTypeMapper VideoTypeMapper { get; }
        private IVideoTypesRepository VideoTypesRepository { get; }
        #endregion
        #region Read
        public IVideoTypeModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return VideoTypeMapper.MapToModel(VideoTypesRepository.Get(id));
        }

        public IVideoTypeModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return VideoTypeMapper.MapToModel(VideoTypesRepository.Get(key));
        }

        public List<IVideoTypeModel> Search(IVideoTypeSearchModel searchModel, bool asListing = false)
        {
            var results = VideoTypesRepository.Search(searchModel);
            return asListing
                ? results.Select(VideoTypeMapper.MapToModelListing).ToList()
                : results.Select(VideoTypeMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IVideoTypeModel Create(IVideoTypeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(VideoTypeMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = VideoTypeMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            VideoTypesRepository.Add(newEntity);
            // Try to Save Changes
            VideoTypesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IVideoTypeModel Update(IVideoTypeModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = VideoTypesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (VideoTypeMapper.AreEqual(model, existingEntity))
            {
                return VideoTypeMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            VideoTypeMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            VideoTypesRepository.Update(VideoTypeMapper.MapToEntity(model));
            // Try to Save Changes
            VideoTypesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VideoTypesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VideoTypesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IVideoType entity)
        {
            // Deactivate it
            VideoTypesRepository.Deactivate(entity);
            // Try to Save Changes
            VideoTypesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = VideoTypesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = VideoTypesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IVideoType entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            VideoTypesRepository.Remove(entity);
            // Try to Save Changes
            VideoTypesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
