// <auto-generated>
// <copyright file="Concepts.IConceptModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the Concept model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Concepts
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ConceptsBusinessWorkflow : IConceptsBusinessWorkflow
    {
        public ConceptsBusinessWorkflow(IConceptsRepository conceptsRepository, IConceptMapper conceptMapper)
        {
            ConceptsRepository = conceptsRepository;
            ConceptMapper = conceptMapper;
        }

        #region Private Variables
        private IConceptMapper ConceptMapper { get; }
        private IConceptsRepository ConceptsRepository { get; }
        #endregion
        #region Read
        public IConceptModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ConceptMapper.MapToModel(ConceptsRepository.Get(id));
        }

        public IConceptModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ConceptMapper.MapToModel(ConceptsRepository.Get(key));
        }

        public List<IConceptModel> Search(IConceptSearchModel searchModel, bool asListing = false)
        {
            var results = ConceptsRepository.Search(searchModel);
            return asListing
                ? results.Select(ConceptMapper.MapToModelListing).ToList()
                : results.Select(ConceptMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IConceptModel Create(IConceptModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ConceptMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ConceptMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ConceptsRepository.Add(newEntity);
            // Try to Save Changes
            ConceptsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IConceptModel Update(IConceptModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ConceptsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ConceptMapper.AreEqual(model, existingEntity))
            {
                return ConceptMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ConceptMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ConceptsRepository.Update(ConceptMapper.MapToEntity(model));
            // Try to Save Changes
            ConceptsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ConceptsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ConceptsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IConcept entity)
        {
            // Deactivate it
            ConceptsRepository.Deactivate(entity);
            // Try to Save Changes
            ConceptsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ConceptsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ConceptsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IConcept entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ConceptsRepository.Remove(entity);
            // Try to Save Changes
            ConceptsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
