// <auto-generated>
// <copyright file="Objects.IObjectIssueModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the ObjectIssue model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Objects
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ObjectIssuesBusinessWorkflow : IObjectIssuesBusinessWorkflow
    {
        public ObjectIssuesBusinessWorkflow(IObjectIssuesRepository objectIssuesRepository, IObjectIssueMapper objectIssueMapper)
        {
            ObjectIssuesRepository = objectIssuesRepository;
            ObjectIssueMapper = objectIssueMapper;
        }

        #region Private Variables
        private IObjectIssueMapper ObjectIssueMapper { get; }
        private IObjectIssuesRepository ObjectIssuesRepository { get; }
        #endregion
        #region Read
        public IObjectIssueModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ObjectIssueMapper.MapToModel(ObjectIssuesRepository.Get(id));
        }

        public IObjectIssueModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ObjectIssueMapper.MapToModel(ObjectIssuesRepository.Get(key));
        }

        public List<IObjectIssueModel> Search(IObjectIssueSearchModel searchModel, bool asListing = false)
        {
            var results = ObjectIssuesRepository.Search(searchModel);
            return asListing
                ? results.Select(ObjectIssueMapper.MapToModelListing).ToList()
                : results.Select(ObjectIssueMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IObjectIssueModel Create(IObjectIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ObjectIssueMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ObjectIssueMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ObjectIssuesRepository.Add(newEntity);
            // Try to Save Changes
            ObjectIssuesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IObjectIssueModel Update(IObjectIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ObjectIssuesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ObjectIssueMapper.AreEqual(model, existingEntity))
            {
                return ObjectIssueMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ObjectIssueMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ObjectIssuesRepository.Update(ObjectIssueMapper.MapToEntity(model));
            // Try to Save Changes
            ObjectIssuesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ObjectIssuesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ObjectIssuesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IObjectIssue entity)
        {
            // Deactivate it
            ObjectIssuesRepository.Deactivate(entity);
            // Try to Save Changes
            ObjectIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ObjectIssuesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ObjectIssuesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IObjectIssue entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ObjectIssuesRepository.Remove(entity);
            // Try to Save Changes
            ObjectIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
