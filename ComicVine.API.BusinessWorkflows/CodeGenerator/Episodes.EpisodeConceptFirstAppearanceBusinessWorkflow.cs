// <auto-generated>
// <copyright file="Episodes.IEpisodeConceptFirstAppearanceModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the EpisodeConceptFirstAppearance model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Episodes
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class EpisodeConceptFirstAppearancesBusinessWorkflow : IEpisodeConceptFirstAppearancesBusinessWorkflow
    {
        public EpisodeConceptFirstAppearancesBusinessWorkflow(IEpisodeConceptFirstAppearancesRepository episodeConceptFirstAppearancesRepository, IEpisodeConceptFirstAppearanceMapper episodeConceptFirstAppearanceMapper)
        {
            EpisodeConceptFirstAppearancesRepository = episodeConceptFirstAppearancesRepository;
            EpisodeConceptFirstAppearanceMapper = episodeConceptFirstAppearanceMapper;
        }

        #region Private Variables
        private IEpisodeConceptFirstAppearanceMapper EpisodeConceptFirstAppearanceMapper { get; }
        private IEpisodeConceptFirstAppearancesRepository EpisodeConceptFirstAppearancesRepository { get; }
        #endregion
        #region Read
        public IEpisodeConceptFirstAppearanceModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return EpisodeConceptFirstAppearanceMapper.MapToModel(EpisodeConceptFirstAppearancesRepository.Get(id));
        }

        public IEpisodeConceptFirstAppearanceModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return EpisodeConceptFirstAppearanceMapper.MapToModel(EpisodeConceptFirstAppearancesRepository.Get(key));
        }

        public List<IEpisodeConceptFirstAppearanceModel> Search(IEpisodeConceptFirstAppearanceSearchModel searchModel, bool asListing = false)
        {
            var results = EpisodeConceptFirstAppearancesRepository.Search(searchModel);
            return asListing
                ? results.Select(EpisodeConceptFirstAppearanceMapper.MapToModelListing).ToList()
                : results.Select(EpisodeConceptFirstAppearanceMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IEpisodeConceptFirstAppearanceModel Create(IEpisodeConceptFirstAppearanceModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(EpisodeConceptFirstAppearanceMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = EpisodeConceptFirstAppearanceMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            EpisodeConceptFirstAppearancesRepository.Add(newEntity);
            // Try to Save Changes
            EpisodeConceptFirstAppearancesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IEpisodeConceptFirstAppearanceModel Update(IEpisodeConceptFirstAppearanceModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = EpisodeConceptFirstAppearancesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (EpisodeConceptFirstAppearanceMapper.AreEqual(model, existingEntity))
            {
                return EpisodeConceptFirstAppearanceMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            EpisodeConceptFirstAppearanceMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            EpisodeConceptFirstAppearancesRepository.Update(EpisodeConceptFirstAppearanceMapper.MapToEntity(model));
            // Try to Save Changes
            EpisodeConceptFirstAppearancesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeConceptFirstAppearancesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeConceptFirstAppearancesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IEpisodeConceptFirstAppearance entity)
        {
            // Deactivate it
            EpisodeConceptFirstAppearancesRepository.Deactivate(entity);
            // Try to Save Changes
            EpisodeConceptFirstAppearancesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = EpisodeConceptFirstAppearancesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = EpisodeConceptFirstAppearancesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IEpisodeConceptFirstAppearance entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            EpisodeConceptFirstAppearancesRepository.Remove(entity);
            // Try to Save Changes
            EpisodeConceptFirstAppearancesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
