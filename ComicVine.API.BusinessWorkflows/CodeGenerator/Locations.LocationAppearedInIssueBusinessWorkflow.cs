// <auto-generated>
// <copyright file="Locations.ILocationAppearedInIssueModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the LocationAppearedInIssue model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Locations
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class LocationAppearedInIssuesBusinessWorkflow : ILocationAppearedInIssuesBusinessWorkflow
    {
        public LocationAppearedInIssuesBusinessWorkflow(ILocationAppearedInIssuesRepository locationAppearedInIssuesRepository, ILocationAppearedInIssueMapper locationAppearedInIssueMapper)
        {
            LocationAppearedInIssuesRepository = locationAppearedInIssuesRepository;
            LocationAppearedInIssueMapper = locationAppearedInIssueMapper;
        }

        #region Private Variables
        private ILocationAppearedInIssueMapper LocationAppearedInIssueMapper { get; }
        private ILocationAppearedInIssuesRepository LocationAppearedInIssuesRepository { get; }
        #endregion
        #region Read
        public ILocationAppearedInIssueModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return LocationAppearedInIssueMapper.MapToModel(LocationAppearedInIssuesRepository.Get(id));
        }

        public ILocationAppearedInIssueModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return LocationAppearedInIssueMapper.MapToModel(LocationAppearedInIssuesRepository.Get(key));
        }

        public List<ILocationAppearedInIssueModel> Search(ILocationAppearedInIssueSearchModel searchModel, bool asListing = false)
        {
            var results = LocationAppearedInIssuesRepository.Search(searchModel);
            return asListing
                ? results.Select(LocationAppearedInIssueMapper.MapToModelListing).ToList()
                : results.Select(LocationAppearedInIssueMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ILocationAppearedInIssueModel Create(ILocationAppearedInIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(LocationAppearedInIssueMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = LocationAppearedInIssueMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            LocationAppearedInIssuesRepository.Add(newEntity);
            // Try to Save Changes
            LocationAppearedInIssuesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ILocationAppearedInIssueModel Update(ILocationAppearedInIssueModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = LocationAppearedInIssuesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (LocationAppearedInIssueMapper.AreEqual(model, existingEntity))
            {
                return LocationAppearedInIssueMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            LocationAppearedInIssueMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            LocationAppearedInIssuesRepository.Update(LocationAppearedInIssueMapper.MapToEntity(model));
            // Try to Save Changes
            LocationAppearedInIssuesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationAppearedInIssuesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationAppearedInIssuesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ILocationAppearedInIssue entity)
        {
            // Deactivate it
            LocationAppearedInIssuesRepository.Deactivate(entity);
            // Try to Save Changes
            LocationAppearedInIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = LocationAppearedInIssuesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = LocationAppearedInIssuesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ILocationAppearedInIssue entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            LocationAppearedInIssuesRepository.Remove(entity);
            // Try to Save Changes
            LocationAppearedInIssuesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
