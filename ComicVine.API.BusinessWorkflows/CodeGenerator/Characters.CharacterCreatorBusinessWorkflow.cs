// <auto-generated>
// <copyright file="Characters.ICharacterCreatorModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the CharacterCreator model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Characters
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class CharacterCreatorsBusinessWorkflow : ICharacterCreatorsBusinessWorkflow
    {
        public CharacterCreatorsBusinessWorkflow(ICharacterCreatorsRepository characterCreatorsRepository, ICharacterCreatorMapper characterCreatorMapper)
        {
            CharacterCreatorsRepository = characterCreatorsRepository;
            CharacterCreatorMapper = characterCreatorMapper;
        }

        #region Private Variables
        private ICharacterCreatorMapper CharacterCreatorMapper { get; }
        private ICharacterCreatorsRepository CharacterCreatorsRepository { get; }
        #endregion
        #region Read
        public ICharacterCreatorModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return CharacterCreatorMapper.MapToModel(CharacterCreatorsRepository.Get(id));
        }

        public ICharacterCreatorModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return CharacterCreatorMapper.MapToModel(CharacterCreatorsRepository.Get(key));
        }

        public List<ICharacterCreatorModel> Search(ICharacterCreatorSearchModel searchModel, bool asListing = false)
        {
            var results = CharacterCreatorsRepository.Search(searchModel);
            return asListing
                ? results.Select(CharacterCreatorMapper.MapToModelListing).ToList()
                : results.Select(CharacterCreatorMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ICharacterCreatorModel Create(ICharacterCreatorModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(CharacterCreatorMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = CharacterCreatorMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            CharacterCreatorsRepository.Add(newEntity);
            // Try to Save Changes
            CharacterCreatorsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ICharacterCreatorModel Update(ICharacterCreatorModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = CharacterCreatorsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (CharacterCreatorMapper.AreEqual(model, existingEntity))
            {
                return CharacterCreatorMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            CharacterCreatorMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            CharacterCreatorsRepository.Update(CharacterCreatorMapper.MapToEntity(model));
            // Try to Save Changes
            CharacterCreatorsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterCreatorsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterCreatorsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ICharacterCreator entity)
        {
            // Deactivate it
            CharacterCreatorsRepository.Deactivate(entity);
            // Try to Save Changes
            CharacterCreatorsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = CharacterCreatorsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = CharacterCreatorsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ICharacterCreator entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            CharacterCreatorsRepository.Remove(entity);
            // Try to Save Changes
            CharacterCreatorsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
