// <auto-generated>
// <copyright file="Teams.ITeamStoryArcModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the TeamStoryArc model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Teams
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class TeamStoryArcsBusinessWorkflow : ITeamStoryArcsBusinessWorkflow
    {
        public TeamStoryArcsBusinessWorkflow(ITeamStoryArcsRepository teamStoryArcsRepository, ITeamStoryArcMapper teamStoryArcMapper)
        {
            TeamStoryArcsRepository = teamStoryArcsRepository;
            TeamStoryArcMapper = teamStoryArcMapper;
        }

        #region Private Variables
        private ITeamStoryArcMapper TeamStoryArcMapper { get; }
        private ITeamStoryArcsRepository TeamStoryArcsRepository { get; }
        #endregion
        #region Read
        public ITeamStoryArcModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return TeamStoryArcMapper.MapToModel(TeamStoryArcsRepository.Get(id));
        }

        public ITeamStoryArcModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return TeamStoryArcMapper.MapToModel(TeamStoryArcsRepository.Get(key));
        }

        public List<ITeamStoryArcModel> Search(ITeamStoryArcSearchModel searchModel, bool asListing = false)
        {
            var results = TeamStoryArcsRepository.Search(searchModel);
            return asListing
                ? results.Select(TeamStoryArcMapper.MapToModelListing).ToList()
                : results.Select(TeamStoryArcMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ITeamStoryArcModel Create(ITeamStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(TeamStoryArcMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = TeamStoryArcMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            TeamStoryArcsRepository.Add(newEntity);
            // Try to Save Changes
            TeamStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ITeamStoryArcModel Update(ITeamStoryArcModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = TeamStoryArcsRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (TeamStoryArcMapper.AreEqual(model, existingEntity))
            {
                return TeamStoryArcMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            TeamStoryArcMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            TeamStoryArcsRepository.Update(TeamStoryArcMapper.MapToEntity(model));
            // Try to Save Changes
            TeamStoryArcsRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = TeamStoryArcsRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = TeamStoryArcsRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ITeamStoryArc entity)
        {
            // Deactivate it
            TeamStoryArcsRepository.Deactivate(entity);
            // Try to Save Changes
            TeamStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = TeamStoryArcsRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = TeamStoryArcsRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ITeamStoryArc entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            TeamStoryArcsRepository.Remove(entity);
            // Try to Save Changes
            TeamStoryArcsRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
