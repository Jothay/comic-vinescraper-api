// <auto-generated>
// <copyright file="Objects.IObjectMovieModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the ObjectMovie model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Objects
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class ObjectMoviesBusinessWorkflow : IObjectMoviesBusinessWorkflow
    {
        public ObjectMoviesBusinessWorkflow(IObjectMoviesRepository objectMoviesRepository, IObjectMovieMapper objectMovieMapper)
        {
            ObjectMoviesRepository = objectMoviesRepository;
            ObjectMovieMapper = objectMovieMapper;
        }

        #region Private Variables
        private IObjectMovieMapper ObjectMovieMapper { get; }
        private IObjectMoviesRepository ObjectMoviesRepository { get; }
        #endregion
        #region Read
        public IObjectMovieModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return ObjectMovieMapper.MapToModel(ObjectMoviesRepository.Get(id));
        }

        public IObjectMovieModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return ObjectMovieMapper.MapToModel(ObjectMoviesRepository.Get(key));
        }

        public List<IObjectMovieModel> Search(IObjectMovieSearchModel searchModel, bool asListing = false)
        {
            var results = ObjectMoviesRepository.Search(searchModel);
            return asListing
                ? results.Select(ObjectMovieMapper.MapToModelListing).ToList()
                : results.Select(ObjectMovieMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public IObjectMovieModel Create(IObjectMovieModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(ObjectMovieMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = ObjectMovieMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            ObjectMoviesRepository.Add(newEntity);
            // Try to Save Changes
            ObjectMoviesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public IObjectMovieModel Update(IObjectMovieModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = ObjectMoviesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (ObjectMovieMapper.AreEqual(model, existingEntity))
            {
                return ObjectMovieMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            ObjectMovieMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            ObjectMoviesRepository.Update(ObjectMovieMapper.MapToEntity(model));
            // Try to Save Changes
            ObjectMoviesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ObjectMoviesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ObjectMoviesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(IObjectMovie entity)
        {
            // Deactivate it
            ObjectMoviesRepository.Deactivate(entity);
            // Try to Save Changes
            ObjectMoviesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = ObjectMoviesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = ObjectMoviesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(IObjectMovie entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            ObjectMoviesRepository.Remove(entity);
            // Try to Save Changes
            ObjectMoviesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
