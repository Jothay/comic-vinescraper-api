// <auto-generated>
// <copyright file="Series.ISeriesAliasModel.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the SeriesAlias model interfaces.</summary>
// <remarks>This file was autogenerated using InterfaceFileMaster.tt in ComicVine.API\Interfaces\00.AutoGenerated</remarks>
// </auto-generated>
namespace ComicVine.API.BusinessWorkflows//.Series
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Interfaces.BusinessWorkflows;
    using Interfaces.DataModels;
    using Interfaces.Mappers;
    using Interfaces.Models;
    using Interfaces.Repositories;
    using Interfaces.SearchModels;

    public class SeriesAliasesBusinessWorkflow : ISeriesAliasesBusinessWorkflow
    {
        public SeriesAliasesBusinessWorkflow(ISeriesAliasesRepository seriesAliasesRepository, ISeriesAliasMapper seriesAliasMapper)
        {
            SeriesAliasesRepository = seriesAliasesRepository;
            SeriesAliasMapper = seriesAliasMapper;
        }

        #region Private Variables
        private ISeriesAliasMapper SeriesAliasMapper { get; }
        private ISeriesAliasesRepository SeriesAliasesRepository { get; }
        #endregion
        #region Read
        public ISeriesAliasModel Get(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            return SeriesAliasMapper.MapToModel(SeriesAliasesRepository.Get(id));
        }

        public ISeriesAliasModel Get(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            return SeriesAliasMapper.MapToModel(SeriesAliasesRepository.Get(key));
        }

        public List<ISeriesAliasModel> Search(ISeriesAliasSearchModel searchModel, bool asListing = false)
        {
            var results = SeriesAliasesRepository.Search(searchModel);
            return asListing
                ? results.Select(SeriesAliasMapper.MapToModelListing).ToList()
                : results.Select(SeriesAliasMapper.MapToModelLite).ToList();
        }
        #endregion
        #region Create
        public ISeriesAliasModel Create(ISeriesAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateIDIsNull(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Search for an Existing Record (Don't allow Duplicates
            var results = Search(SeriesAliasMapper.MapToSearchModel(model));
            if (results.Any()) { return results.First(); } // Return the first that matches
            // Map model to a new entity
            var newEntity = SeriesAliasMapper.MapToEntity(model);
            newEntity.CreatedDate = BusinessWorkflowBase.GenDateTime;
            newEntity.UpdatedDate = null;
            newEntity.Active = true;
            // Add it
            SeriesAliasesRepository.Add(newEntity);
            // Try to Save Changes
            SeriesAliasesRepository.SaveChanges();
            // Return the new value
            return Get(newEntity.Id);
        }
        #endregion
        #region Update
        public ISeriesAliasModel Update(ISeriesAliasModel model)
        {
            // Validate model
            BusinessWorkflowBase.ValidateRequiredNullableID(model.Id);
            //BusinessWorkflowBase.ValidateRequiredString(model.Name, nameof(model.Name));
            // Find existing entity
            // ReSharper disable once PossibleInvalidOperationException
            var existingEntity = SeriesAliasesRepository.Get(model.Id.Value);
            // Check if we would be applying identical information, if we are, just return the original
            // ReSharper disable once SuspiciousTypeConversion.Global
            if (SeriesAliasMapper.AreEqual(model, existingEntity))
            {
                return SeriesAliasMapper.MapToModel(existingEntity);
            }
            // Map model to an existing entity
            SeriesAliasMapper.MapToEntity(model, ref existingEntity);
            existingEntity.UpdatedDate = BusinessWorkflowBase.GenDateTime;
            // Update it
            SeriesAliasesRepository.Update(SeriesAliasMapper.MapToEntity(model));
            // Try to Save Changes
            SeriesAliasesRepository.SaveChanges();
            // Return the new value
            return Get(existingEntity.Id);
        }
        #endregion
        #region Deactivate
        public bool Deactivate(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = SeriesAliasesRepository.Get(id);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with id {id} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        public bool Deactivate(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = SeriesAliasesRepository.Get(key);
            if (existingEntity == null)
            {
                throw new InvalidOperationException($"Could not find an entity with key {key} to deactivate it");
            }
            // Do the Deactivate
            return Deactivate(existingEntity);
        }
        protected bool Deactivate(ISeriesAlias entity)
        {
            // Deactivate it
            SeriesAliasesRepository.Deactivate(entity);
            // Try to Save Changes
            SeriesAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
        #region Remove
        public bool Remove(int id)
        {
            BusinessWorkflowBase.ValidateRequiredID(id);
            // Find existing Entity
            var existingEntity = SeriesAliasesRepository.Get(id);
            // Do the Remove
            return Remove(existingEntity);
        }
        public bool Remove(string key)
        {
            BusinessWorkflowBase.ValidateRequiredKey(key);
            // Find existing Entity
            var existingEntity = SeriesAliasesRepository.Get(key);
            // Do the Remove
            return Remove(existingEntity);
        }
        protected bool Remove(ISeriesAlias entity)
        {
            if (entity == null) { return true; } // No entity found to remove, consider it passed
            // Remove it
            SeriesAliasesRepository.Remove(entity);
            // Try to Save Changes
            SeriesAliasesRepository.SaveChanges();
            // Finished!
            return true;
        }
        #endregion
    }
}
