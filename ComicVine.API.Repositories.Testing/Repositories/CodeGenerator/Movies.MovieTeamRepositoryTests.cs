// <auto-generated>
// <copyright file="Movies.IMovieTeamRepository.cs" company="clarityventures.com">
// Copyright (c) 2015 Clarity Ventures, Inc. All rights reserved.
// </copyright>
// <summary>Implements the MovieTeamsRepository Class.</summary>
// <remarks>This file was autogenerated using RepositoryTestingFileMaster.tt in ComicVine.API.Repositories.Testing\Repositories\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVine.API.Testing.Repositories.Movies
{
    using System.Collections.Generic;
    using System.Data.Entity;
    using System.Linq;
    using API.Repositories;
    using DataModel.Schema;
    using Interfaces.SearchModels;
    using Moq;
    using SearchModels;
    using Xunit;

    public static class MovieTeamsMockingSetup
    {
        public static Mock<DataModel.ModelEntities> DoMockingSetupForContext(bool includeMovieTeamsData, out Mock<IDbSet<MovieTeam>> mockSetMovieTeams, bool markAMovieTeamInactive = false)
        {
            var createDate = new System.DateTime(2015, 05, 28, 10, 45, 00);
            var mockContext = new Mock<DataModel.ModelEntities>();
            // Apply Data and set up IQueryable
            mockSetMovieTeams = new Mock<IDbSet<MovieTeam>>();
            var personData = (!includeMovieTeamsData ? new List<MovieTeam>() : new List<MovieTeam>
            {
                new MovieTeam { Id = 1, Active = !markAMovieTeamInactive, CreatedDate = createDate, CustomKey = "KING-STEPHEN", ApiDetailUrl = "/TEST/KING-STEPHEN", },
                new MovieTeam { Id = 2, Active = true, CreatedDate = createDate.AddDays(1), CustomKey = "NIVEN-LARRY", ApiDetailUrl = "/TEST/NIVEN-LARRY", }
            }).AsQueryable();
            mockSetMovieTeams.As<IQueryable<MovieTeam>>().Setup(m => m.Provider).Returns(personData.Provider);
            mockSetMovieTeams.As<IQueryable<MovieTeam>>().Setup(m => m.Expression).Returns(personData.Expression);
            mockSetMovieTeams.As<IQueryable<MovieTeam>>().Setup(m => m.ElementType).Returns(personData.ElementType);
            mockSetMovieTeams.As<IQueryable<MovieTeam>>().Setup(m => m.GetEnumerator()).Returns(personData.GetEnumerator());
            mockContext.Setup(m => m.MovieTeams).Returns(mockSetMovieTeams.Object);
            mockContext.Setup(m => m.SetModified(It.IsAny<object>())).Callback(() => { });
            // Return
            return mockContext;
        }
    }

    public class MovieTeamsRepositoryTests
    {
        [Fact]
        public void Verify_Add_Should_AddTheEntityToTheContext()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(false, out mockSetMovieTeams);
            var repository = new MovieTeamsRepository(mockContext.Object);
            var movieTeams = new MovieTeam { Active = true, CustomKey = "SALVATORE-RAA", };
            // Act
            repository.Add(movieTeams);
            // Assert
            mockSetMovieTeams.Verify(x => x.Add(movieTeams), Times.Once);
        }

        [Fact]
        public void Verify_Remove_Should_RemoveTheEntityFromTheContext()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams);
            var repository = new MovieTeamsRepository(mockContext.Object);
            var movieTeams = repository.Get(1);
            // Act
            repository.Remove(movieTeams);
            // Assert
            mockSetMovieTeams.Verify(x => x.Remove((MovieTeam)movieTeams), Times.Once);
        }

        [Fact]
        public void Verify_Deactivate_Should_SetTheActivePropertyToFalseOnTheEntity()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams);
            var repository = new MovieTeamsRepository(mockContext.Object);
            var movieTeams = repository.Get(1);
            // Act
            repository.Deactivate(movieTeams);
            // Assert
            Assert.Equal(false, movieTeams.Active);
        }

        [Fact]
        public void Verify_Update_Should_SetTheEntityStateToModified()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams);
            var repository = new MovieTeamsRepository(mockContext.Object);
            var movieTeams = repository.Get(1);
            movieTeams.ApiDetailUrl = "/TEST";
            // Act
            repository.Update(movieTeams);
            // Assert
            mockContext.Verify(x => x.SetModified(It.IsAny<object>()), Times.Once);
        }

        [Fact]
        public void Verify_Get_ByID_Should_ReturnTheCorrectMovieTeam()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams);
            var repository = new MovieTeamsRepository(mockContext.Object);
            // Act
            var movieTeams = repository.Get(1);
            // Assert
                        Assert.Equal("/TEST/KING-STEPHEN", movieTeams.ApiDetailUrl);
        }

        [Fact]
        public void Verify_Get_ByKey_Should_ReturnTheCorrectMovieTeam()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams);
            var repository = new MovieTeamsRepository(mockContext.Object);
            // Act
            var movieTeams = repository.Get("KING-STEPHEN");
            // Assert
                        Assert.Equal("/TEST/KING-STEPHEN", movieTeams.ApiDetailUrl);
        }

        [Fact]
        public void Verify_List_Should_ReturnAListOfMovieTeamsWithData()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams);
            var repository = new MovieTeamsRepository(mockContext.Object);
            // Act
            var movieTeams = repository.List.ToArray();
            // Assert
            Assert.Equal(2, movieTeams.Length);
            Assert.Equal(1, movieTeams[0].Id);
            Assert.Equal("/TEST/KING-STEPHEN", movieTeams[0].ApiDetailUrl);
            Assert.Equal(2, movieTeams[1].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", movieTeams[1].ApiDetailUrl);
        }

        [Fact]
        public void Verify_Search_Should_ReturnAListOfMovieTeamsWithDataMatchingSearchParameters()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams, true);
            var repository = new MovieTeamsRepository(mockContext.Object);
            var searchModel = new Mock<IMovieTeamSearchModel>();
            // Act
            var movieTeams = repository.Search(searchModel.Object).ToArray();
            // Assert
            Assert.Equal(1, movieTeams.Length);
            Assert.Equal(2, movieTeams[0].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", movieTeams[0].ApiDetailUrl);
            // Stephen King was filtered out because he was Inactive
        }

        [Fact]
        public void Verify_Search_WithSelectStatement_Should_ReturnAListOfDynamicObjects()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams, true);
            var repository = new MovieTeamsRepository(mockContext.Object);
            var searchModel = new Mock<IMovieTeamSearchModel>();
            // Act
            var movieTeams = repository.Search(searchModel.Object, i => new { i.Id, MyApiDetailUrl = i.ApiDetailUrl }).ToArray();
            // Assert
            Assert.Equal(1, movieTeams.Length);
            Assert.Equal(2, movieTeams[0].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", movieTeams[0].MyApiDetailUrl);
            // Stephen King was filtered out because he was Inactive
        }

        [Fact]
        public void Verify_Search_WithModifiedSince_Should_ReturnAListOfMovieTeamsWithDataMatchingSearchParameters()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams, true);
            var repository = new MovieTeamsRepository(mockContext.Object);
            var createDate = new System.DateTime(2015, 05, 28, 10, 45, 00).AddDays(1).AddMinutes(-1);
            //var searchModel = new Mock<IMovieTeamSearchModel>();
            //searchModel.Setup(m => m.ModifiedSince).Returns(() => createDate);
            var searchModel = new MovieTeamSearchModel { ModifiedSince = createDate };
            // Act
            var movieTeams = repository.Search(searchModel/*.Object*/).ToArray();
            // Assert
            Assert.Equal(1, movieTeams.Length);
            Assert.Equal(2, movieTeams[0].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", movieTeams[0].ApiDetailUrl);
            // Stephen King was filtered out because he was created before the modified since date
        }

        [Fact]
        public void Verify_Search_WithPaging_Should_ReturnAListOfMovieTeamsWithDataMatchingSearchParameters()
        {
            // Arrange
            Mock<IDbSet<MovieTeam>> mockSetMovieTeams;
            var mockContext = MovieTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetMovieTeams);
            var repository = new MovieTeamsRepository(mockContext.Object);
            var searchModel = new MovieTeamSearchModel { Paging = new Paging { Skip = 1, Take = 1 } };
            // Act
            var movieTeams = repository.Search(searchModel/*.Object*/).ToArray();
            // Assert
            Assert.Equal(1, movieTeams.Length);
            Assert.Equal(2, movieTeams[0].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", movieTeams[0].ApiDetailUrl);
            // Stephen King was filtered out because he was Skipped
        }
    }
}
