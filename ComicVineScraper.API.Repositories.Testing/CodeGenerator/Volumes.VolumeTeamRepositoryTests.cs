// <auto-generated>
// <copyright file="Volumes.VolumeTeamRepositoryTests.cs" company="jothay.com">
// Copyright (c) 2015 Jothay Tech, Inc. All rights reserved.
// </copyright>
// <summary>Declares the VolumeTeamsRepositoryTests class.</summary>
// <remarks>This file was auto-generated using RepositoryTestingFileMaster.tt in
// ComicVineScraper.API.Repositories.Testing\CodeGenerator</remarks>
// </auto-generated>
namespace ComicVineScraper.API.Testing.Repositories.Volumes
{
    using System.Collections.Generic;
    using System.Data.Entity;
    using System.Linq;
    using API.Repositories;
    using DataModel.Schema;
    using Interfaces.SearchModels;
    using Moq;
    using SearchModels;
    using Xunit;

    public static class VolumeTeamsMockingSetup
    {
        public static Mock<DataModel.ModelEntities> DoMockingSetupForContext(bool includeVolumeTeamsData, out Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams, bool markAVolumeTeamInactive = false)
        {
            var createDate = new System.DateTime(2015, 05, 28, 10, 45, 00);
            var mockContext = new Mock<DataModel.ModelEntities>();
            // Apply Data and set up IQueryable
            mockSetVolumeTeams = new Mock<IDbSet<VolumeTeam>>();
            var personData = (!includeVolumeTeamsData ? new List<VolumeTeam>() : new List<VolumeTeam>
            {
                new VolumeTeam { Id = 1, Active = !markAVolumeTeamInactive, CreatedDate = createDate, CustomKey = "KING-STEPHEN", ApiDetailUrl = "/TEST/KING-STEPHEN", },
                new VolumeTeam { Id = 2, Active = true, CreatedDate = createDate.AddDays(1), CustomKey = "NIVEN-LARRY", ApiDetailUrl = "/TEST/NIVEN-LARRY", }
            }).AsQueryable();
            mockSetVolumeTeams.As<IQueryable<VolumeTeam>>().Setup(m => m.Provider).Returns(personData.Provider);
            mockSetVolumeTeams.As<IQueryable<VolumeTeam>>().Setup(m => m.Expression).Returns(personData.Expression);
            mockSetVolumeTeams.As<IQueryable<VolumeTeam>>().Setup(m => m.ElementType).Returns(personData.ElementType);
            mockSetVolumeTeams.As<IQueryable<VolumeTeam>>().Setup(m => m.GetEnumerator()).Returns(personData.GetEnumerator());
            mockContext.Setup(m => m.VolumeTeams).Returns(mockSetVolumeTeams.Object);
            mockContext.Setup(m => m.SetModified(It.IsAny<object>())).Callback(() => { });
            // Return
            return mockContext;
        }
    }

    public class VolumeTeamsRepositoryTests
    {
        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Add_Should_AddTheEntityToTheContext()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(false, out mockSetVolumeTeams);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            var volumeTeams = new VolumeTeam { Active = true, CustomKey = "SALVATORE-RAA", };
            // Act
            repository.Add(volumeTeams);
            // Assert
            mockSetVolumeTeams.Verify(x => x.Add(volumeTeams), Times.Once);
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Remove_Should_RemoveTheEntityFromTheContext()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            var volumeTeams = repository.Get(1);
            // Act
            repository.Remove(volumeTeams);
            // Assert
            mockSetVolumeTeams.Verify(x => x.Remove((VolumeTeam)volumeTeams), Times.Once);
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Deactivate_Should_SetTheActivePropertyToFalseOnTheEntity()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            var volumeTeams = repository.Get(1);
            // Act
            repository.Deactivate(volumeTeams);
            // Assert
            Assert.Equal(false, volumeTeams.Active);
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Update_Should_SetTheEntityStateToModified()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            var volumeTeams = repository.Get(1);
            volumeTeams.ApiDetailUrl = "/TEST";
            // Act
            repository.Update(volumeTeams);
            // Assert
            mockContext.Verify(x => x.SetModified(It.IsAny<object>()), Times.Once);
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Get_ByID_Should_ReturnTheCorrectVolumeTeam()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            // Act
            var volumeTeams = repository.Get(1);
            // Assert
                        Assert.Equal("/TEST/KING-STEPHEN", volumeTeams.ApiDetailUrl);
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Get_ByKey_Should_ReturnTheCorrectVolumeTeam()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            // Act
            var volumeTeams = repository.Get("KING-STEPHEN");
            // Assert
                        Assert.Equal("/TEST/KING-STEPHEN", volumeTeams.ApiDetailUrl);
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_List_Should_ReturnAListOfVolumeTeamsWithData()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            // Act
            var volumeTeams = repository.List.ToArray();
            // Assert
            Assert.Equal(2, volumeTeams.Length);
            Assert.Equal(1, volumeTeams[0].Id);
            Assert.Equal("/TEST/KING-STEPHEN", volumeTeams[0].ApiDetailUrl);
            Assert.Equal(2, volumeTeams[1].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", volumeTeams[1].ApiDetailUrl);
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Search_Should_ReturnAListOfVolumeTeamsWithDataMatchingSearchParameters()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams, true);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            var searchModel = new Mock<IVolumeTeamSearchModel>();
            // Act
            var volumeTeams = repository.Search(searchModel.Object).ToArray();
            // Assert
            Assert.Equal(1, volumeTeams.Length);
            Assert.Equal(2, volumeTeams[0].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", volumeTeams[0].ApiDetailUrl);
            // Stephen King was filtered out because he was Inactive
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Search_WithSelectStatement_Should_ReturnAListOfDynamicObjects()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams, true);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            var searchModel = new Mock<IVolumeTeamSearchModel>();
            // Act
            var volumeTeams = repository.Search(searchModel.Object, i => new { i.Id, MyApiDetailUrl = i.ApiDetailUrl }).ToArray();
            // Assert
            Assert.Equal(1, volumeTeams.Length);
            Assert.Equal(2, volumeTeams[0].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", volumeTeams[0].MyApiDetailUrl);
            // Stephen King was filtered out because he was Inactive
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Search_WithModifiedSince_Should_ReturnAListOfVolumeTeamsWithDataMatchingSearchParameters()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams, true);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            var createDate = new System.DateTime(2015, 05, 28, 10, 45, 00).AddDays(1).AddMinutes(-1);
            //var searchModel = new Mock<IVolumeTeamSearchModel>();
            //searchModel.Setup(m => m.ModifiedSince).Returns(() => createDate);
            var searchModel = new VolumeTeamSearchModel { ModifiedSince = createDate };
            // Act
            var volumeTeams = repository.Search(searchModel/*.Object*/).ToArray();
            // Assert
            Assert.Equal(1, volumeTeams.Length);
            Assert.Equal(2, volumeTeams[0].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", volumeTeams[0].ApiDetailUrl);
            // Stephen King was filtered out because he was created before the modified since date
        }

        [Fact][Trait("Category", "VolumeTeams")]
        public void Verify_Search_WithPaging_Should_ReturnAListOfVolumeTeamsWithDataMatchingSearchParameters()
        {
            // Arrange
            Mock<IDbSet<VolumeTeam>> mockSetVolumeTeams;
            var mockContext = VolumeTeamsMockingSetup.DoMockingSetupForContext(true, out mockSetVolumeTeams);
            var repository = new VolumeTeamsRepository(mockContext.Object);
            var searchModel = new VolumeTeamSearchModel { Paging = new Paging { Skip = 1, Take = 1 } };
            // Act
            var volumeTeams = repository.Search(searchModel/*.Object*/).ToArray();
            // Assert
            Assert.Equal(1, volumeTeams.Length);
            Assert.Equal(2, volumeTeams[0].Id);
            Assert.Equal("/TEST/NIVEN-LARRY", volumeTeams[0].ApiDetailUrl);
            // Stephen King was filtered out because he was Skipped
        }
    }
}
